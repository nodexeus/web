type StatusState = 'default' | 'active' | 'inactive' | 'error';

// type NodeState = 'syncing' | 'upgrading' | 'synced' | 'consensus' | 'stopped';

type HostState = 'pending' | 'normal' | 'loaded' | 'issue';

type LoadingState = 'initializing' | 'loading' | 'finished';
