import { css } from '@emotion/react';

export const styles = {
  base: css`
    background: var(--color-background);
    padding: 40px 0;
  `,
  actions: css`
    display: flex;
    justify-content: space-between;
    align-items: center;
  `,
};
