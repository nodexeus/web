import { css } from '@emotion/react';

export const styles = {
  base: css`
    position: relative;
    max-width: max-content;
  `,
  dropdown: css`
    top: 44px;
    right: 5px;
    left: 0;
    min-width: 190px;
    box-shadow: 0 0 10px rgb(0 0 0 / 30%);
  `,
};
