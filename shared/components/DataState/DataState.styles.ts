import { css } from '@emotion/react';

export const styles = {
  base: css`
    display: inline-flex;
    align-items: center;
    gap: 8px;

    svg {
      flex-basis: 12px;
      flex-shrink: 0;
    }
  `,
};
