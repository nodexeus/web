import styled from "@emotion/styled";

const StyledWrapper = styled.main`
    width: 56px;
    border-right: 1px solid #363938;
`;

export default () => {
  return (
   <StyledWrapper>
    Left
   </StyledWrapper>
  );
}
