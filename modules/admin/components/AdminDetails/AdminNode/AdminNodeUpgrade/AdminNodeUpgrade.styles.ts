import { css } from '@emotion/react';
import { ITheme } from 'types/theme';

export const styles = {
  wrapper: (theme: ITheme) => css`
    width: 40px;
  `,
  menu: css`
    width: 140px;
    top: 48px;
  `,
};
