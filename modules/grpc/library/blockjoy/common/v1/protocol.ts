// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v3.19.1
// source: blockjoy/common/v1/protocol.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "blockjoy.common.v1";

/** The type of field to display in the UI. */
export enum UiType {
  UI_TYPE_UNSPECIFIED = 0,
  /** UI_TYPE_SWITCH - A checkbox in the UI (type: boolean) */
  UI_TYPE_SWITCH = 1,
  /** UI_TYPE_TEXT - A text field in the UI (type: utf-8 string). */
  UI_TYPE_TEXT = 2,
  /** UI_TYPE_PASSWORD - A text field with hidden content in the UI (type: utf-8 string). */
  UI_TYPE_PASSWORD = 3,
  /** UI_TYPE_ENUM - A set of allowed values (type: enum). */
  UI_TYPE_ENUM = 4,
  UNRECOGNIZED = -1,
}

/** The visibility of some protocol, version or image. */
export enum Visibility {
  VISIBILITY_UNSPECIFIED = 0,
  VISIBILITY_PUBLIC = 1,
  VISIBILITY_PRIVATE = 2,
  VISIBILITY_DEVELOPMENT = 3,
  UNRECOGNIZED = -1,
}

/**
 * A key identifier to some protocol version.
 *
 * The string representation is: "{protocol_key}/{variant_key}"
 */
export interface ProtocolVersionKey {
  /** The key identifier to the protocol (in lower-kebab-case). */
  protocolKey: string;
  /** The key identifier to the protocol variant (in lower-kebab-case). */
  variantKey: string;
}

/** Metadata associated with a `ProtocolVersionKey`. */
export interface VersionMetadata {
  /** The lookup key for metadata (e.g. `network`). */
  metadataKey: string;
  /** The value of the metadata key (e.g. `mainnet`). */
  value: string;
  /** A readable description of what the key and value represent. */
  description?: string | undefined;
}

function createBaseProtocolVersionKey(): ProtocolVersionKey {
  return { protocolKey: "", variantKey: "" };
}

export const ProtocolVersionKey: MessageFns<ProtocolVersionKey> = {
  encode(message: ProtocolVersionKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolKey !== "") {
      writer.uint32(10).string(message.protocolKey);
    }
    if (message.variantKey !== "") {
      writer.uint32(18).string(message.variantKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProtocolVersionKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProtocolVersionKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.protocolKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.variantKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ProtocolVersionKey>): ProtocolVersionKey {
    return ProtocolVersionKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProtocolVersionKey>): ProtocolVersionKey {
    const message = createBaseProtocolVersionKey();
    message.protocolKey = object.protocolKey ?? "";
    message.variantKey = object.variantKey ?? "";
    return message;
  },
};

function createBaseVersionMetadata(): VersionMetadata {
  return { metadataKey: "", value: "", description: undefined };
}

export const VersionMetadata: MessageFns<VersionMetadata> = {
  encode(message: VersionMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadataKey !== "") {
      writer.uint32(10).string(message.metadataKey);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    if (message.description !== undefined) {
      writer.uint32(26).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VersionMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVersionMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadataKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<VersionMetadata>): VersionMetadata {
    return VersionMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VersionMetadata>): VersionMetadata {
    const message = createBaseVersionMetadata();
    message.metadataKey = object.metadataKey ?? "";
    message.value = object.value ?? "";
    message.description = object.description ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
