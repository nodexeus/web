// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: blockjoy/v1/archive.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "blockjoy.v1";

export interface ArchiveServiceGetDownloadMetadataRequest {
  /** An identifier to some property-set archive. */
  archiveId: string;
  /** Whether this is a public or private archive. */
  orgId?:
    | string
    | undefined;
  /** The data version (or latest if none). */
  dataVersion?: number | undefined;
}

export interface ArchiveServiceGetDownloadMetadataResponse {
  /** The data version of the download manifest. */
  dataVersion: number;
  /** The total size (in bytes) of the uncompressed data. */
  totalSize: number;
  /** The chunk compression type. */
  compression?:
    | Compression
    | undefined;
  /** The total number of download manifest chunks. */
  chunks: number;
}

export interface ArchiveServiceGetDownloadChunksRequest {
  /** An identifier to some property-set archive. */
  archiveId: string;
  /** Whether this is a public or private archive. */
  orgId?:
    | string
    | undefined;
  /** The data version for the download manifest. */
  dataVersion: number;
  /** The set of chunk indexes to generate download URLs for. */
  chunkIndexes: number[];
}

export interface ArchiveServiceGetDownloadChunksResponse {
  /** Presigned URLs for the requested download manifest chunk indexes. */
  chunks: ArchiveChunk[];
}

export interface ArchiveServiceGetUploadSlotsRequest {
  /** An identifier to some property-set archive. */
  archiveId: string;
  /** Whether this is a public or private archive. */
  orgId?:
    | string
    | undefined;
  /** The data version. If not set, will increment the last data version. */
  dataVersion?:
    | number
    | undefined;
  /** Expiry duration of an upload URL (in seconds). */
  urlExpires?:
    | number
    | undefined;
  /** The requested data slot indexes to generate upload URLs for. */
  slotIndexes: number[];
}

/** R2.5: Request to get upload slots using store key for multi-client uploads */
export interface ArchiveServiceGetUploadSlotsByStoreKeyRequest {
  /** The store key for this client's archive */
  storeKey: string;
  /** Whether this is a public or private archive. */
  orgId?:
    | string
    | undefined;
  /** The data version. If not set, will increment the last data version. */
  dataVersion?:
    | number
    | undefined;
  /** Expiry duration of an upload URL (in seconds). */
  urlExpires?:
    | number
    | undefined;
  /** The requested data slot indexes to generate upload URLs for. */
  slotIndexes: number[];
}

/** Request to get download metadata using store key for multi-client downloads */
export interface ArchiveServiceGetDownloadMetadataByStoreKeyRequest {
  /** The store key for this client's archive */
  storeKey: string;
  /** Whether this is a public or private archive. */
  orgId?:
    | string
    | undefined;
  /** The data version (or latest if none). */
  dataVersion?: number | undefined;
}

/** Request to get download chunks using store key for multi-client downloads */
export interface ArchiveServiceGetDownloadChunksByStoreKeyRequest {
  /** The store key for this client's archive */
  storeKey: string;
  /** Whether this is a public or private archive. */
  orgId?:
    | string
    | undefined;
  /** The data version for the download manifest. */
  dataVersion: number;
  /** The set of chunk indexes to generate download URLs for. */
  chunkIndexes: number[];
}

export interface ArchiveServiceGetUploadSlotsResponse {
  /** The data version of the upload slots. */
  dataVersion: number;
  /** A list of slots for uploading archive chunks. */
  slots: UploadSlot[];
}

/** An upload slot for a chunk of data. */
export interface UploadSlot {
  /** The index of this upload slot. */
  index: number;
  /** The persistent key to an archive chunk. */
  key: string;
  /** A temporary, pre-signed upload url for the data. */
  url: string;
}

export interface ArchiveServicePutDownloadManifestRequest {
  /** An identifier to some property-set archive. */
  archiveId: string;
  /** Whether this is a public or private archive. */
  orgId?:
    | string
    | undefined;
  /** The data version to save. */
  dataVersion: number;
  /** The total size (in bytes) of the uncompressed data. */
  totalSize: number;
  /** The chunk compression type. */
  compression?:
    | Compression
    | undefined;
  /** Metadata for each archive data chunk. */
  chunks: ArchiveChunk[];
}

/** Request to put download manifest using store key for multi-client uploads */
export interface ArchiveServicePutDownloadManifestByStoreKeyRequest {
  /** The store key for this client's archive */
  storeKey: string;
  /** Whether this is a public or private archive. */
  orgId?:
    | string
    | undefined;
  /** The data version to save. */
  dataVersion: number;
  /** The total size (in bytes) of the uncompressed data. */
  totalSize: number;
  /** The chunk compression type. */
  compression?:
    | Compression
    | undefined;
  /** Metadata for each archive data chunk. */
  chunks: ArchiveChunk[];
  /** R4.2: The image ID this archive belongs to (for multi-client support) */
  imageId?:
    | string
    | undefined;
  /** R4.2: The client name (e.g., "reth", "lighthouse") - derived from store_key if not provided */
  clientName?:
    | string
    | undefined;
  /** R4.2: The data directory path - derived from store_key if not provided */
  dataDirectory?: string | undefined;
}

export interface ArchiveServicePutDownloadManifestResponse {
}

/** Metadata describing an archive chunk URL and file mapping. */
export interface ArchiveChunk {
  /** The index of this archive chunk. */
  index: number;
  /** The persistent chunk key. */
  key: string;
  /** A temporary pre-signed download url (or none for a blueprint). */
  url?:
    | string
    | undefined;
  /** The checksum of the chunk data. */
  checksum:
    | Checksum
    | undefined;
  /** The chunk data size (in bytes). */
  size: number;
  /** Mappings from chunks to file paths. */
  destinations: ChunkTarget[];
}

/** The target file for a decompressed data chunk. */
export interface ChunkTarget {
  /** The relative file path. */
  path: string;
  /** The start position of the data bytes in the target file. */
  positionBytes: number;
  /** The size of the uncompressed chunk data. */
  sizeBytes: number;
}

/** A checksum consisting of a type and value. */
export interface Checksum {
  sha1?: Uint8Array | undefined;
  sha256?: Uint8Array | undefined;
  blake3?: Uint8Array | undefined;
}

/** Type of compression used on chunk data. */
export interface Compression {
  /** ZStandard compression, where the value is the compression level. */
  zstd?: number | undefined;
}

/** R4.2: Request to get download metadata for a specific client */
export interface GetDownloadMetadataForClientRequest {
  /** The client name (e.g., "reth", "lighthouse") */
  clientName: string;
  /** The store key for this client's archive */
  storeKey: string;
  /** Whether this is a public or private archive. */
  orgId?:
    | string
    | undefined;
  /** The data version (or latest if none). */
  dataVersion?: number | undefined;
}

/** R4.2: Request to discover all client archives for an image */
export interface DiscoverClientArchivesRequest {
  /** The image ID to discover client archives for */
  imageId: string;
  /** Whether this is a public or private archive. */
  orgId?: string | undefined;
}

/** R4.2: Response containing all client archives for an image */
export interface DiscoverClientArchivesResponse {
  clientArchives: ClientArchive[];
}

/** R4.2: Information about a client-specific archive */
export interface ClientArchive {
  clientName: string;
  storeKey: string;
  dataDirectory: string;
}

/** Request for S3 configuration */
export interface GetS3ConfigRequest {
  /** Optional: specify if you want read-only or read-write access */
  readOnly?: boolean | undefined;
}

/** Response with S3 configuration and temporary credentials */
export interface GetS3ConfigResponse {
  /** S3 bucket name */
  bucket: string;
  /** AWS region */
  region: string;
  /** Temporary access key ID */
  accessKeyId: string;
  /** Temporary secret access key */
  secretAccessKey: string;
  /** Session token for temporary credentials */
  sessionToken?:
    | string
    | undefined;
  /** Expiration time for the credentials (in seconds from now) */
  expiresIn?:
    | number
    | undefined;
  /** Custom endpoint URL for S3-compatible services (e.g., MinIO, R2) */
  endpointUrl?: string | undefined;
}

function createBaseArchiveServiceGetDownloadMetadataRequest(): ArchiveServiceGetDownloadMetadataRequest {
  return { archiveId: "", orgId: undefined, dataVersion: undefined };
}

export const ArchiveServiceGetDownloadMetadataRequest: MessageFns<ArchiveServiceGetDownloadMetadataRequest> = {
  encode(message: ArchiveServiceGetDownloadMetadataRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.archiveId !== "") {
      writer.uint32(10).string(message.archiveId);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    if (message.dataVersion !== undefined) {
      writer.uint32(24).uint64(message.dataVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServiceGetDownloadMetadataRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServiceGetDownloadMetadataRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.archiveId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchiveServiceGetDownloadMetadataRequest>): ArchiveServiceGetDownloadMetadataRequest {
    return ArchiveServiceGetDownloadMetadataRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchiveServiceGetDownloadMetadataRequest>): ArchiveServiceGetDownloadMetadataRequest {
    const message = createBaseArchiveServiceGetDownloadMetadataRequest();
    message.archiveId = object.archiveId ?? "";
    message.orgId = object.orgId ?? undefined;
    message.dataVersion = object.dataVersion ?? undefined;
    return message;
  },
};

function createBaseArchiveServiceGetDownloadMetadataResponse(): ArchiveServiceGetDownloadMetadataResponse {
  return { dataVersion: 0, totalSize: 0, compression: undefined, chunks: 0 };
}

export const ArchiveServiceGetDownloadMetadataResponse: MessageFns<ArchiveServiceGetDownloadMetadataResponse> = {
  encode(message: ArchiveServiceGetDownloadMetadataResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dataVersion !== 0) {
      writer.uint32(8).uint64(message.dataVersion);
    }
    if (message.totalSize !== 0) {
      writer.uint32(16).uint64(message.totalSize);
    }
    if (message.compression !== undefined) {
      Compression.encode(message.compression, writer.uint32(26).fork()).join();
    }
    if (message.chunks !== 0) {
      writer.uint32(32).uint32(message.chunks);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServiceGetDownloadMetadataResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServiceGetDownloadMetadataResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalSize = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.compression = Compression.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.chunks = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchiveServiceGetDownloadMetadataResponse>): ArchiveServiceGetDownloadMetadataResponse {
    return ArchiveServiceGetDownloadMetadataResponse.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<ArchiveServiceGetDownloadMetadataResponse>,
  ): ArchiveServiceGetDownloadMetadataResponse {
    const message = createBaseArchiveServiceGetDownloadMetadataResponse();
    message.dataVersion = object.dataVersion ?? 0;
    message.totalSize = object.totalSize ?? 0;
    message.compression = (object.compression !== undefined && object.compression !== null)
      ? Compression.fromPartial(object.compression)
      : undefined;
    message.chunks = object.chunks ?? 0;
    return message;
  },
};

function createBaseArchiveServiceGetDownloadChunksRequest(): ArchiveServiceGetDownloadChunksRequest {
  return { archiveId: "", orgId: undefined, dataVersion: 0, chunkIndexes: [] };
}

export const ArchiveServiceGetDownloadChunksRequest: MessageFns<ArchiveServiceGetDownloadChunksRequest> = {
  encode(message: ArchiveServiceGetDownloadChunksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.archiveId !== "") {
      writer.uint32(10).string(message.archiveId);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    if (message.dataVersion !== 0) {
      writer.uint32(24).uint64(message.dataVersion);
    }
    writer.uint32(34).fork();
    for (const v of message.chunkIndexes) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServiceGetDownloadChunksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServiceGetDownloadChunksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.archiveId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.chunkIndexes.push(reader.uint32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.chunkIndexes.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchiveServiceGetDownloadChunksRequest>): ArchiveServiceGetDownloadChunksRequest {
    return ArchiveServiceGetDownloadChunksRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchiveServiceGetDownloadChunksRequest>): ArchiveServiceGetDownloadChunksRequest {
    const message = createBaseArchiveServiceGetDownloadChunksRequest();
    message.archiveId = object.archiveId ?? "";
    message.orgId = object.orgId ?? undefined;
    message.dataVersion = object.dataVersion ?? 0;
    message.chunkIndexes = object.chunkIndexes?.map((e) => e) || [];
    return message;
  },
};

function createBaseArchiveServiceGetDownloadChunksResponse(): ArchiveServiceGetDownloadChunksResponse {
  return { chunks: [] };
}

export const ArchiveServiceGetDownloadChunksResponse: MessageFns<ArchiveServiceGetDownloadChunksResponse> = {
  encode(message: ArchiveServiceGetDownloadChunksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.chunks) {
      ArchiveChunk.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServiceGetDownloadChunksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServiceGetDownloadChunksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.chunks.push(ArchiveChunk.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchiveServiceGetDownloadChunksResponse>): ArchiveServiceGetDownloadChunksResponse {
    return ArchiveServiceGetDownloadChunksResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchiveServiceGetDownloadChunksResponse>): ArchiveServiceGetDownloadChunksResponse {
    const message = createBaseArchiveServiceGetDownloadChunksResponse();
    message.chunks = object.chunks?.map((e) => ArchiveChunk.fromPartial(e)) || [];
    return message;
  },
};

function createBaseArchiveServiceGetUploadSlotsRequest(): ArchiveServiceGetUploadSlotsRequest {
  return { archiveId: "", orgId: undefined, dataVersion: undefined, urlExpires: undefined, slotIndexes: [] };
}

export const ArchiveServiceGetUploadSlotsRequest: MessageFns<ArchiveServiceGetUploadSlotsRequest> = {
  encode(message: ArchiveServiceGetUploadSlotsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.archiveId !== "") {
      writer.uint32(10).string(message.archiveId);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    if (message.dataVersion !== undefined) {
      writer.uint32(24).uint64(message.dataVersion);
    }
    if (message.urlExpires !== undefined) {
      writer.uint32(32).uint32(message.urlExpires);
    }
    writer.uint32(42).fork();
    for (const v of message.slotIndexes) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServiceGetUploadSlotsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServiceGetUploadSlotsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.archiveId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.urlExpires = reader.uint32();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.slotIndexes.push(reader.uint32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.slotIndexes.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchiveServiceGetUploadSlotsRequest>): ArchiveServiceGetUploadSlotsRequest {
    return ArchiveServiceGetUploadSlotsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchiveServiceGetUploadSlotsRequest>): ArchiveServiceGetUploadSlotsRequest {
    const message = createBaseArchiveServiceGetUploadSlotsRequest();
    message.archiveId = object.archiveId ?? "";
    message.orgId = object.orgId ?? undefined;
    message.dataVersion = object.dataVersion ?? undefined;
    message.urlExpires = object.urlExpires ?? undefined;
    message.slotIndexes = object.slotIndexes?.map((e) => e) || [];
    return message;
  },
};

function createBaseArchiveServiceGetUploadSlotsByStoreKeyRequest(): ArchiveServiceGetUploadSlotsByStoreKeyRequest {
  return { storeKey: "", orgId: undefined, dataVersion: undefined, urlExpires: undefined, slotIndexes: [] };
}

export const ArchiveServiceGetUploadSlotsByStoreKeyRequest: MessageFns<ArchiveServiceGetUploadSlotsByStoreKeyRequest> =
  {
    encode(
      message: ArchiveServiceGetUploadSlotsByStoreKeyRequest,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.storeKey !== "") {
        writer.uint32(10).string(message.storeKey);
      }
      if (message.orgId !== undefined) {
        writer.uint32(18).string(message.orgId);
      }
      if (message.dataVersion !== undefined) {
        writer.uint32(24).uint64(message.dataVersion);
      }
      if (message.urlExpires !== undefined) {
        writer.uint32(32).uint32(message.urlExpires);
      }
      writer.uint32(42).fork();
      for (const v of message.slotIndexes) {
        writer.uint32(v);
      }
      writer.join();
      return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServiceGetUploadSlotsByStoreKeyRequest {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseArchiveServiceGetUploadSlotsByStoreKeyRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.storeKey = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.orgId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.dataVersion = longToNumber(reader.uint64());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.urlExpires = reader.uint32();
            continue;
          }
          case 5: {
            if (tag === 40) {
              message.slotIndexes.push(reader.uint32());

              continue;
            }

            if (tag === 42) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.slotIndexes.push(reader.uint32());
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    create(
      base?: DeepPartial<ArchiveServiceGetUploadSlotsByStoreKeyRequest>,
    ): ArchiveServiceGetUploadSlotsByStoreKeyRequest {
      return ArchiveServiceGetUploadSlotsByStoreKeyRequest.fromPartial(base ?? {});
    },
    fromPartial(
      object: DeepPartial<ArchiveServiceGetUploadSlotsByStoreKeyRequest>,
    ): ArchiveServiceGetUploadSlotsByStoreKeyRequest {
      const message = createBaseArchiveServiceGetUploadSlotsByStoreKeyRequest();
      message.storeKey = object.storeKey ?? "";
      message.orgId = object.orgId ?? undefined;
      message.dataVersion = object.dataVersion ?? undefined;
      message.urlExpires = object.urlExpires ?? undefined;
      message.slotIndexes = object.slotIndexes?.map((e) => e) || [];
      return message;
    },
  };

function createBaseArchiveServiceGetDownloadMetadataByStoreKeyRequest(): ArchiveServiceGetDownloadMetadataByStoreKeyRequest {
  return { storeKey: "", orgId: undefined, dataVersion: undefined };
}

export const ArchiveServiceGetDownloadMetadataByStoreKeyRequest: MessageFns<
  ArchiveServiceGetDownloadMetadataByStoreKeyRequest
> = {
  encode(
    message: ArchiveServiceGetDownloadMetadataByStoreKeyRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.storeKey !== "") {
      writer.uint32(10).string(message.storeKey);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    if (message.dataVersion !== undefined) {
      writer.uint32(24).uint64(message.dataVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServiceGetDownloadMetadataByStoreKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServiceGetDownloadMetadataByStoreKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storeKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(
    base?: DeepPartial<ArchiveServiceGetDownloadMetadataByStoreKeyRequest>,
  ): ArchiveServiceGetDownloadMetadataByStoreKeyRequest {
    return ArchiveServiceGetDownloadMetadataByStoreKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<ArchiveServiceGetDownloadMetadataByStoreKeyRequest>,
  ): ArchiveServiceGetDownloadMetadataByStoreKeyRequest {
    const message = createBaseArchiveServiceGetDownloadMetadataByStoreKeyRequest();
    message.storeKey = object.storeKey ?? "";
    message.orgId = object.orgId ?? undefined;
    message.dataVersion = object.dataVersion ?? undefined;
    return message;
  },
};

function createBaseArchiveServiceGetDownloadChunksByStoreKeyRequest(): ArchiveServiceGetDownloadChunksByStoreKeyRequest {
  return { storeKey: "", orgId: undefined, dataVersion: 0, chunkIndexes: [] };
}

export const ArchiveServiceGetDownloadChunksByStoreKeyRequest: MessageFns<
  ArchiveServiceGetDownloadChunksByStoreKeyRequest
> = {
  encode(
    message: ArchiveServiceGetDownloadChunksByStoreKeyRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.storeKey !== "") {
      writer.uint32(10).string(message.storeKey);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    if (message.dataVersion !== 0) {
      writer.uint32(24).uint64(message.dataVersion);
    }
    writer.uint32(34).fork();
    for (const v of message.chunkIndexes) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServiceGetDownloadChunksByStoreKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServiceGetDownloadChunksByStoreKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storeKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.chunkIndexes.push(reader.uint32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.chunkIndexes.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(
    base?: DeepPartial<ArchiveServiceGetDownloadChunksByStoreKeyRequest>,
  ): ArchiveServiceGetDownloadChunksByStoreKeyRequest {
    return ArchiveServiceGetDownloadChunksByStoreKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<ArchiveServiceGetDownloadChunksByStoreKeyRequest>,
  ): ArchiveServiceGetDownloadChunksByStoreKeyRequest {
    const message = createBaseArchiveServiceGetDownloadChunksByStoreKeyRequest();
    message.storeKey = object.storeKey ?? "";
    message.orgId = object.orgId ?? undefined;
    message.dataVersion = object.dataVersion ?? 0;
    message.chunkIndexes = object.chunkIndexes?.map((e) => e) || [];
    return message;
  },
};

function createBaseArchiveServiceGetUploadSlotsResponse(): ArchiveServiceGetUploadSlotsResponse {
  return { dataVersion: 0, slots: [] };
}

export const ArchiveServiceGetUploadSlotsResponse: MessageFns<ArchiveServiceGetUploadSlotsResponse> = {
  encode(message: ArchiveServiceGetUploadSlotsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dataVersion !== 0) {
      writer.uint32(8).uint64(message.dataVersion);
    }
    for (const v of message.slots) {
      UploadSlot.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServiceGetUploadSlotsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServiceGetUploadSlotsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.slots.push(UploadSlot.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchiveServiceGetUploadSlotsResponse>): ArchiveServiceGetUploadSlotsResponse {
    return ArchiveServiceGetUploadSlotsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchiveServiceGetUploadSlotsResponse>): ArchiveServiceGetUploadSlotsResponse {
    const message = createBaseArchiveServiceGetUploadSlotsResponse();
    message.dataVersion = object.dataVersion ?? 0;
    message.slots = object.slots?.map((e) => UploadSlot.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUploadSlot(): UploadSlot {
  return { index: 0, key: "", url: "" };
}

export const UploadSlot: MessageFns<UploadSlot> = {
  encode(message: UploadSlot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    if (message.url !== "") {
      writer.uint32(26).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadSlot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadSlot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<UploadSlot>): UploadSlot {
    return UploadSlot.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadSlot>): UploadSlot {
    const message = createBaseUploadSlot();
    message.index = object.index ?? 0;
    message.key = object.key ?? "";
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseArchiveServicePutDownloadManifestRequest(): ArchiveServicePutDownloadManifestRequest {
  return { archiveId: "", orgId: undefined, dataVersion: 0, totalSize: 0, compression: undefined, chunks: [] };
}

export const ArchiveServicePutDownloadManifestRequest: MessageFns<ArchiveServicePutDownloadManifestRequest> = {
  encode(message: ArchiveServicePutDownloadManifestRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.archiveId !== "") {
      writer.uint32(10).string(message.archiveId);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    if (message.dataVersion !== 0) {
      writer.uint32(24).uint64(message.dataVersion);
    }
    if (message.totalSize !== 0) {
      writer.uint32(32).uint64(message.totalSize);
    }
    if (message.compression !== undefined) {
      Compression.encode(message.compression, writer.uint32(42).fork()).join();
    }
    for (const v of message.chunks) {
      ArchiveChunk.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServicePutDownloadManifestRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServicePutDownloadManifestRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.archiveId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalSize = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.compression = Compression.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.chunks.push(ArchiveChunk.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchiveServicePutDownloadManifestRequest>): ArchiveServicePutDownloadManifestRequest {
    return ArchiveServicePutDownloadManifestRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchiveServicePutDownloadManifestRequest>): ArchiveServicePutDownloadManifestRequest {
    const message = createBaseArchiveServicePutDownloadManifestRequest();
    message.archiveId = object.archiveId ?? "";
    message.orgId = object.orgId ?? undefined;
    message.dataVersion = object.dataVersion ?? 0;
    message.totalSize = object.totalSize ?? 0;
    message.compression = (object.compression !== undefined && object.compression !== null)
      ? Compression.fromPartial(object.compression)
      : undefined;
    message.chunks = object.chunks?.map((e) => ArchiveChunk.fromPartial(e)) || [];
    return message;
  },
};

function createBaseArchiveServicePutDownloadManifestByStoreKeyRequest(): ArchiveServicePutDownloadManifestByStoreKeyRequest {
  return {
    storeKey: "",
    orgId: undefined,
    dataVersion: 0,
    totalSize: 0,
    compression: undefined,
    chunks: [],
    imageId: undefined,
    clientName: undefined,
    dataDirectory: undefined,
  };
}

export const ArchiveServicePutDownloadManifestByStoreKeyRequest: MessageFns<
  ArchiveServicePutDownloadManifestByStoreKeyRequest
> = {
  encode(
    message: ArchiveServicePutDownloadManifestByStoreKeyRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.storeKey !== "") {
      writer.uint32(10).string(message.storeKey);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    if (message.dataVersion !== 0) {
      writer.uint32(24).uint64(message.dataVersion);
    }
    if (message.totalSize !== 0) {
      writer.uint32(32).uint64(message.totalSize);
    }
    if (message.compression !== undefined) {
      Compression.encode(message.compression, writer.uint32(42).fork()).join();
    }
    for (const v of message.chunks) {
      ArchiveChunk.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.imageId !== undefined) {
      writer.uint32(58).string(message.imageId);
    }
    if (message.clientName !== undefined) {
      writer.uint32(66).string(message.clientName);
    }
    if (message.dataDirectory !== undefined) {
      writer.uint32(74).string(message.dataDirectory);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServicePutDownloadManifestByStoreKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServicePutDownloadManifestByStoreKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storeKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalSize = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.compression = Compression.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.chunks.push(ArchiveChunk.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.clientName = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.dataDirectory = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(
    base?: DeepPartial<ArchiveServicePutDownloadManifestByStoreKeyRequest>,
  ): ArchiveServicePutDownloadManifestByStoreKeyRequest {
    return ArchiveServicePutDownloadManifestByStoreKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<ArchiveServicePutDownloadManifestByStoreKeyRequest>,
  ): ArchiveServicePutDownloadManifestByStoreKeyRequest {
    const message = createBaseArchiveServicePutDownloadManifestByStoreKeyRequest();
    message.storeKey = object.storeKey ?? "";
    message.orgId = object.orgId ?? undefined;
    message.dataVersion = object.dataVersion ?? 0;
    message.totalSize = object.totalSize ?? 0;
    message.compression = (object.compression !== undefined && object.compression !== null)
      ? Compression.fromPartial(object.compression)
      : undefined;
    message.chunks = object.chunks?.map((e) => ArchiveChunk.fromPartial(e)) || [];
    message.imageId = object.imageId ?? undefined;
    message.clientName = object.clientName ?? undefined;
    message.dataDirectory = object.dataDirectory ?? undefined;
    return message;
  },
};

function createBaseArchiveServicePutDownloadManifestResponse(): ArchiveServicePutDownloadManifestResponse {
  return {};
}

export const ArchiveServicePutDownloadManifestResponse: MessageFns<ArchiveServicePutDownloadManifestResponse> = {
  encode(_: ArchiveServicePutDownloadManifestResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveServicePutDownloadManifestResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveServicePutDownloadManifestResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchiveServicePutDownloadManifestResponse>): ArchiveServicePutDownloadManifestResponse {
    return ArchiveServicePutDownloadManifestResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ArchiveServicePutDownloadManifestResponse>): ArchiveServicePutDownloadManifestResponse {
    const message = createBaseArchiveServicePutDownloadManifestResponse();
    return message;
  },
};

function createBaseArchiveChunk(): ArchiveChunk {
  return { index: 0, key: "", url: undefined, checksum: undefined, size: 0, destinations: [] };
}

export const ArchiveChunk: MessageFns<ArchiveChunk> = {
  encode(message: ArchiveChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    if (message.url !== undefined) {
      writer.uint32(26).string(message.url);
    }
    if (message.checksum !== undefined) {
      Checksum.encode(message.checksum, writer.uint32(34).fork()).join();
    }
    if (message.size !== 0) {
      writer.uint32(40).uint64(message.size);
    }
    for (const v of message.destinations) {
      ChunkTarget.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.checksum = Checksum.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.size = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.destinations.push(ChunkTarget.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchiveChunk>): ArchiveChunk {
    return ArchiveChunk.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchiveChunk>): ArchiveChunk {
    const message = createBaseArchiveChunk();
    message.index = object.index ?? 0;
    message.key = object.key ?? "";
    message.url = object.url ?? undefined;
    message.checksum = (object.checksum !== undefined && object.checksum !== null)
      ? Checksum.fromPartial(object.checksum)
      : undefined;
    message.size = object.size ?? 0;
    message.destinations = object.destinations?.map((e) => ChunkTarget.fromPartial(e)) || [];
    return message;
  },
};

function createBaseChunkTarget(): ChunkTarget {
  return { path: "", positionBytes: 0, sizeBytes: 0 };
}

export const ChunkTarget: MessageFns<ChunkTarget> = {
  encode(message: ChunkTarget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.positionBytes !== 0) {
      writer.uint32(16).uint64(message.positionBytes);
    }
    if (message.sizeBytes !== 0) {
      writer.uint32(24).uint64(message.sizeBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChunkTarget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChunkTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.positionBytes = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sizeBytes = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ChunkTarget>): ChunkTarget {
    return ChunkTarget.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ChunkTarget>): ChunkTarget {
    const message = createBaseChunkTarget();
    message.path = object.path ?? "";
    message.positionBytes = object.positionBytes ?? 0;
    message.sizeBytes = object.sizeBytes ?? 0;
    return message;
  },
};

function createBaseChecksum(): Checksum {
  return { sha1: undefined, sha256: undefined, blake3: undefined };
}

export const Checksum: MessageFns<Checksum> = {
  encode(message: Checksum, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sha1 !== undefined) {
      writer.uint32(10).bytes(message.sha1);
    }
    if (message.sha256 !== undefined) {
      writer.uint32(18).bytes(message.sha256);
    }
    if (message.blake3 !== undefined) {
      writer.uint32(26).bytes(message.blake3);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Checksum {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChecksum();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sha1 = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sha256 = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.blake3 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Checksum>): Checksum {
    return Checksum.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Checksum>): Checksum {
    const message = createBaseChecksum();
    message.sha1 = object.sha1 ?? undefined;
    message.sha256 = object.sha256 ?? undefined;
    message.blake3 = object.blake3 ?? undefined;
    return message;
  },
};

function createBaseCompression(): Compression {
  return { zstd: undefined };
}

export const Compression: MessageFns<Compression> = {
  encode(message: Compression, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.zstd !== undefined) {
      writer.uint32(8).int32(message.zstd);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Compression {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompression();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.zstd = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Compression>): Compression {
    return Compression.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Compression>): Compression {
    const message = createBaseCompression();
    message.zstd = object.zstd ?? undefined;
    return message;
  },
};

function createBaseGetDownloadMetadataForClientRequest(): GetDownloadMetadataForClientRequest {
  return { clientName: "", storeKey: "", orgId: undefined, dataVersion: undefined };
}

export const GetDownloadMetadataForClientRequest: MessageFns<GetDownloadMetadataForClientRequest> = {
  encode(message: GetDownloadMetadataForClientRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientName !== "") {
      writer.uint32(10).string(message.clientName);
    }
    if (message.storeKey !== "") {
      writer.uint32(18).string(message.storeKey);
    }
    if (message.orgId !== undefined) {
      writer.uint32(26).string(message.orgId);
    }
    if (message.dataVersion !== undefined) {
      writer.uint32(32).uint64(message.dataVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDownloadMetadataForClientRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDownloadMetadataForClientRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.storeKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.dataVersion = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetDownloadMetadataForClientRequest>): GetDownloadMetadataForClientRequest {
    return GetDownloadMetadataForClientRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetDownloadMetadataForClientRequest>): GetDownloadMetadataForClientRequest {
    const message = createBaseGetDownloadMetadataForClientRequest();
    message.clientName = object.clientName ?? "";
    message.storeKey = object.storeKey ?? "";
    message.orgId = object.orgId ?? undefined;
    message.dataVersion = object.dataVersion ?? undefined;
    return message;
  },
};

function createBaseDiscoverClientArchivesRequest(): DiscoverClientArchivesRequest {
  return { imageId: "", orgId: undefined };
}

export const DiscoverClientArchivesRequest: MessageFns<DiscoverClientArchivesRequest> = {
  encode(message: DiscoverClientArchivesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageId !== "") {
      writer.uint32(10).string(message.imageId);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscoverClientArchivesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscoverClientArchivesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<DiscoverClientArchivesRequest>): DiscoverClientArchivesRequest {
    return DiscoverClientArchivesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DiscoverClientArchivesRequest>): DiscoverClientArchivesRequest {
    const message = createBaseDiscoverClientArchivesRequest();
    message.imageId = object.imageId ?? "";
    message.orgId = object.orgId ?? undefined;
    return message;
  },
};

function createBaseDiscoverClientArchivesResponse(): DiscoverClientArchivesResponse {
  return { clientArchives: [] };
}

export const DiscoverClientArchivesResponse: MessageFns<DiscoverClientArchivesResponse> = {
  encode(message: DiscoverClientArchivesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.clientArchives) {
      ClientArchive.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscoverClientArchivesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscoverClientArchivesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientArchives.push(ClientArchive.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<DiscoverClientArchivesResponse>): DiscoverClientArchivesResponse {
    return DiscoverClientArchivesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DiscoverClientArchivesResponse>): DiscoverClientArchivesResponse {
    const message = createBaseDiscoverClientArchivesResponse();
    message.clientArchives = object.clientArchives?.map((e) => ClientArchive.fromPartial(e)) || [];
    return message;
  },
};

function createBaseClientArchive(): ClientArchive {
  return { clientName: "", storeKey: "", dataDirectory: "" };
}

export const ClientArchive: MessageFns<ClientArchive> = {
  encode(message: ClientArchive, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientName !== "") {
      writer.uint32(10).string(message.clientName);
    }
    if (message.storeKey !== "") {
      writer.uint32(18).string(message.storeKey);
    }
    if (message.dataDirectory !== "") {
      writer.uint32(26).string(message.dataDirectory);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientArchive {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientArchive();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.storeKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dataDirectory = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ClientArchive>): ClientArchive {
    return ClientArchive.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClientArchive>): ClientArchive {
    const message = createBaseClientArchive();
    message.clientName = object.clientName ?? "";
    message.storeKey = object.storeKey ?? "";
    message.dataDirectory = object.dataDirectory ?? "";
    return message;
  },
};

function createBaseGetS3ConfigRequest(): GetS3ConfigRequest {
  return { readOnly: undefined };
}

export const GetS3ConfigRequest: MessageFns<GetS3ConfigRequest> = {
  encode(message: GetS3ConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.readOnly !== undefined) {
      writer.uint32(8).bool(message.readOnly);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetS3ConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetS3ConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.readOnly = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetS3ConfigRequest>): GetS3ConfigRequest {
    return GetS3ConfigRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetS3ConfigRequest>): GetS3ConfigRequest {
    const message = createBaseGetS3ConfigRequest();
    message.readOnly = object.readOnly ?? undefined;
    return message;
  },
};

function createBaseGetS3ConfigResponse(): GetS3ConfigResponse {
  return {
    bucket: "",
    region: "",
    accessKeyId: "",
    secretAccessKey: "",
    sessionToken: undefined,
    expiresIn: undefined,
    endpointUrl: undefined,
  };
}

export const GetS3ConfigResponse: MessageFns<GetS3ConfigResponse> = {
  encode(message: GetS3ConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bucket !== "") {
      writer.uint32(10).string(message.bucket);
    }
    if (message.region !== "") {
      writer.uint32(18).string(message.region);
    }
    if (message.accessKeyId !== "") {
      writer.uint32(26).string(message.accessKeyId);
    }
    if (message.secretAccessKey !== "") {
      writer.uint32(34).string(message.secretAccessKey);
    }
    if (message.sessionToken !== undefined) {
      writer.uint32(42).string(message.sessionToken);
    }
    if (message.expiresIn !== undefined) {
      writer.uint32(48).uint32(message.expiresIn);
    }
    if (message.endpointUrl !== undefined) {
      writer.uint32(58).string(message.endpointUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetS3ConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetS3ConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bucket = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.region = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.accessKeyId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.secretAccessKey = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sessionToken = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.expiresIn = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.endpointUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetS3ConfigResponse>): GetS3ConfigResponse {
    return GetS3ConfigResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetS3ConfigResponse>): GetS3ConfigResponse {
    const message = createBaseGetS3ConfigResponse();
    message.bucket = object.bucket ?? "";
    message.region = object.region ?? "";
    message.accessKeyId = object.accessKeyId ?? "";
    message.secretAccessKey = object.secretAccessKey ?? "";
    message.sessionToken = object.sessionToken ?? undefined;
    message.expiresIn = object.expiresIn ?? undefined;
    message.endpointUrl = object.endpointUrl ?? undefined;
    return message;
  },
};

/** Service to manage the archived data of different protocols. */
export type ArchiveServiceDefinition = typeof ArchiveServiceDefinition;
export const ArchiveServiceDefinition = {
  name: "ArchiveService",
  fullName: "blockjoy.v1.ArchiveService",
  methods: {
    /** Get the download manifest metadata for some image. */
    getDownloadMetadata: {
      name: "GetDownloadMetadata",
      requestType: ArchiveServiceGetDownloadMetadataRequest as typeof ArchiveServiceGetDownloadMetadataRequest,
      requestStream: false,
      responseType: ArchiveServiceGetDownloadMetadataResponse as typeof ArchiveServiceGetDownloadMetadataResponse,
      responseStream: false,
      options: {},
    },
    /** Get presigned download URLs for download manifest data chunks. */
    getDownloadChunks: {
      name: "GetDownloadChunks",
      requestType: ArchiveServiceGetDownloadChunksRequest as typeof ArchiveServiceGetDownloadChunksRequest,
      requestStream: false,
      responseType: ArchiveServiceGetDownloadChunksResponse as typeof ArchiveServiceGetDownloadChunksResponse,
      responseStream: false,
      options: {},
    },
    /** Get presigned upload URLs for archive data chunks. */
    getUploadSlots: {
      name: "GetUploadSlots",
      requestType: ArchiveServiceGetUploadSlotsRequest as typeof ArchiveServiceGetUploadSlotsRequest,
      requestStream: false,
      responseType: ArchiveServiceGetUploadSlotsResponse as typeof ArchiveServiceGetUploadSlotsResponse,
      responseStream: false,
      options: {},
    },
    /** Get presigned upload URLs for archive data chunks using store key. */
    getUploadSlotsByStoreKey: {
      name: "GetUploadSlotsByStoreKey",
      requestType:
        ArchiveServiceGetUploadSlotsByStoreKeyRequest as typeof ArchiveServiceGetUploadSlotsByStoreKeyRequest,
      requestStream: false,
      responseType: ArchiveServiceGetUploadSlotsResponse as typeof ArchiveServiceGetUploadSlotsResponse,
      responseStream: false,
      options: {},
    },
    /** Get the download manifest metadata using store key. */
    getDownloadMetadataByStoreKey: {
      name: "GetDownloadMetadataByStoreKey",
      requestType:
        ArchiveServiceGetDownloadMetadataByStoreKeyRequest as typeof ArchiveServiceGetDownloadMetadataByStoreKeyRequest,
      requestStream: false,
      responseType: ArchiveServiceGetDownloadMetadataResponse as typeof ArchiveServiceGetDownloadMetadataResponse,
      responseStream: false,
      options: {},
    },
    /** Get presigned download URLs for download manifest data chunks using store key. */
    getDownloadChunksByStoreKey: {
      name: "GetDownloadChunksByStoreKey",
      requestType:
        ArchiveServiceGetDownloadChunksByStoreKeyRequest as typeof ArchiveServiceGetDownloadChunksByStoreKeyRequest,
      requestStream: false,
      responseType: ArchiveServiceGetDownloadChunksResponse as typeof ArchiveServiceGetDownloadChunksResponse,
      responseStream: false,
      options: {},
    },
    /** Put a new download manifest for retrieval with `GetDownloadManifest`. */
    putDownloadManifest: {
      name: "PutDownloadManifest",
      requestType: ArchiveServicePutDownloadManifestRequest as typeof ArchiveServicePutDownloadManifestRequest,
      requestStream: false,
      responseType: ArchiveServicePutDownloadManifestResponse as typeof ArchiveServicePutDownloadManifestResponse,
      responseStream: false,
      options: {},
    },
    /** Put a new download manifest using store key for multi-client uploads. */
    putDownloadManifestByStoreKey: {
      name: "PutDownloadManifestByStoreKey",
      requestType:
        ArchiveServicePutDownloadManifestByStoreKeyRequest as typeof ArchiveServicePutDownloadManifestByStoreKeyRequest,
      requestStream: false,
      responseType: ArchiveServicePutDownloadManifestResponse as typeof ArchiveServicePutDownloadManifestResponse,
      responseStream: false,
      options: {},
    },
    /** R4.2: Get download metadata for specific client */
    getDownloadMetadataForClient: {
      name: "GetDownloadMetadataForClient",
      requestType: GetDownloadMetadataForClientRequest as typeof GetDownloadMetadataForClientRequest,
      requestStream: false,
      responseType: ArchiveServiceGetDownloadMetadataResponse as typeof ArchiveServiceGetDownloadMetadataResponse,
      responseStream: false,
      options: {},
    },
    /** R4.2: Discover available archives for all clients in an image */
    discoverClientArchives: {
      name: "DiscoverClientArchives",
      requestType: DiscoverClientArchivesRequest as typeof DiscoverClientArchivesRequest,
      requestStream: false,
      responseType: DiscoverClientArchivesResponse as typeof DiscoverClientArchivesResponse,
      responseStream: false,
      options: {},
    },
    /** Get S3 configuration for direct bucket access to discover snapshots */
    getS3Config: {
      name: "GetS3Config",
      requestType: GetS3ConfigRequest as typeof GetS3ConfigRequest,
      requestStream: false,
      responseType: GetS3ConfigResponse as typeof GetS3ConfigResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface ArchiveServiceImplementation<CallContextExt = {}> {
  /** Get the download manifest metadata for some image. */
  getDownloadMetadata(
    request: ArchiveServiceGetDownloadMetadataRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ArchiveServiceGetDownloadMetadataResponse>>;
  /** Get presigned download URLs for download manifest data chunks. */
  getDownloadChunks(
    request: ArchiveServiceGetDownloadChunksRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ArchiveServiceGetDownloadChunksResponse>>;
  /** Get presigned upload URLs for archive data chunks. */
  getUploadSlots(
    request: ArchiveServiceGetUploadSlotsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ArchiveServiceGetUploadSlotsResponse>>;
  /** Get presigned upload URLs for archive data chunks using store key. */
  getUploadSlotsByStoreKey(
    request: ArchiveServiceGetUploadSlotsByStoreKeyRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ArchiveServiceGetUploadSlotsResponse>>;
  /** Get the download manifest metadata using store key. */
  getDownloadMetadataByStoreKey(
    request: ArchiveServiceGetDownloadMetadataByStoreKeyRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ArchiveServiceGetDownloadMetadataResponse>>;
  /** Get presigned download URLs for download manifest data chunks using store key. */
  getDownloadChunksByStoreKey(
    request: ArchiveServiceGetDownloadChunksByStoreKeyRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ArchiveServiceGetDownloadChunksResponse>>;
  /** Put a new download manifest for retrieval with `GetDownloadManifest`. */
  putDownloadManifest(
    request: ArchiveServicePutDownloadManifestRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ArchiveServicePutDownloadManifestResponse>>;
  /** Put a new download manifest using store key for multi-client uploads. */
  putDownloadManifestByStoreKey(
    request: ArchiveServicePutDownloadManifestByStoreKeyRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ArchiveServicePutDownloadManifestResponse>>;
  /** R4.2: Get download metadata for specific client */
  getDownloadMetadataForClient(
    request: GetDownloadMetadataForClientRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ArchiveServiceGetDownloadMetadataResponse>>;
  /** R4.2: Discover available archives for all clients in an image */
  discoverClientArchives(
    request: DiscoverClientArchivesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<DiscoverClientArchivesResponse>>;
  /** Get S3 configuration for direct bucket access to discover snapshots */
  getS3Config(
    request: GetS3ConfigRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetS3ConfigResponse>>;
}

export interface ArchiveServiceClient<CallOptionsExt = {}> {
  /** Get the download manifest metadata for some image. */
  getDownloadMetadata(
    request: DeepPartial<ArchiveServiceGetDownloadMetadataRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ArchiveServiceGetDownloadMetadataResponse>;
  /** Get presigned download URLs for download manifest data chunks. */
  getDownloadChunks(
    request: DeepPartial<ArchiveServiceGetDownloadChunksRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ArchiveServiceGetDownloadChunksResponse>;
  /** Get presigned upload URLs for archive data chunks. */
  getUploadSlots(
    request: DeepPartial<ArchiveServiceGetUploadSlotsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ArchiveServiceGetUploadSlotsResponse>;
  /** Get presigned upload URLs for archive data chunks using store key. */
  getUploadSlotsByStoreKey(
    request: DeepPartial<ArchiveServiceGetUploadSlotsByStoreKeyRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ArchiveServiceGetUploadSlotsResponse>;
  /** Get the download manifest metadata using store key. */
  getDownloadMetadataByStoreKey(
    request: DeepPartial<ArchiveServiceGetDownloadMetadataByStoreKeyRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ArchiveServiceGetDownloadMetadataResponse>;
  /** Get presigned download URLs for download manifest data chunks using store key. */
  getDownloadChunksByStoreKey(
    request: DeepPartial<ArchiveServiceGetDownloadChunksByStoreKeyRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ArchiveServiceGetDownloadChunksResponse>;
  /** Put a new download manifest for retrieval with `GetDownloadManifest`. */
  putDownloadManifest(
    request: DeepPartial<ArchiveServicePutDownloadManifestRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ArchiveServicePutDownloadManifestResponse>;
  /** Put a new download manifest using store key for multi-client uploads. */
  putDownloadManifestByStoreKey(
    request: DeepPartial<ArchiveServicePutDownloadManifestByStoreKeyRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ArchiveServicePutDownloadManifestResponse>;
  /** R4.2: Get download metadata for specific client */
  getDownloadMetadataForClient(
    request: DeepPartial<GetDownloadMetadataForClientRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ArchiveServiceGetDownloadMetadataResponse>;
  /** R4.2: Discover available archives for all clients in an image */
  discoverClientArchives(
    request: DeepPartial<DiscoverClientArchivesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<DiscoverClientArchivesResponse>;
  /** Get S3 configuration for direct bucket access to discover snapshots */
  getS3Config(
    request: DeepPartial<GetS3ConfigRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetS3ConfigResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
