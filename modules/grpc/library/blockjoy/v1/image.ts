// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v3.19.1
// source: blockjoy/v1/image.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Empty } from "../../google/protobuf/empty";
import { Timestamp } from "../../google/protobuf/timestamp";
import { FirewallConfig, RamdiskConfig } from "../common/v1/config";
import { ProtocolVersionKey, UiType, Visibility } from "../common/v1/protocol";

export const protobufPackage = "blockjoy.v1";

/** An image build for some protocol version. */
export interface Image {
  imageId: string;
  protocolVersionId: string;
  orgId?: string | undefined;
  buildVersion: number;
  imageUri: string;
  description?: string | undefined;
  properties: ImageProperty[];
  firewall: FirewallConfig | undefined;
  minCpuCores: number;
  minMemoryBytes: number;
  minDiskBytes: number;
  minBabelVersion: string;
  ramdisks: RamdiskConfig[];
  visibility: Visibility;
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
  dnsScheme?: string | undefined;
}

/** A config property of an image. */
export interface ImageProperty {
  /** The unique identifier to this property. */
  imagePropertyId: string;
  /** The applicable image for this property. */
  imageId: string;
  /** The lookup key for this property. */
  key: string;
  /** A repeated key to group switches and enums. */
  keyGroup?:
    | string
    | undefined;
  /** For repeated key groups, one value must be the default. */
  isGroupDefault?:
    | boolean
    | undefined;
  /** Whether this property requires different archive data. */
  newArchive: boolean;
  /** The default value for this property. */
  defaultValue: string;
  /** Whether the value may change at runtime. */
  dynamicValue: boolean;
  /** How this field should be displayed in the UI. */
  uiType: UiType;
  /** The display name of this property. */
  displayName?:
    | string
    | undefined;
  /** The display name of the property group. */
  displayGroup?:
    | string
    | undefined;
  /** Additional information describing this property. */
  description?:
    | string
    | undefined;
  /** Additional CPU cores needed when using this property. */
  addCpuCores?:
    | number
    | undefined;
  /** Additional memory bytes needed when using this property. */
  addMemoryBytes?:
    | number
    | undefined;
  /** Additional disk bytes needed when using this property. */
  addDiskBytes?: number | undefined;
}

/** An identifier to archive data in external storage. */
export interface Archive {
  archiveId: string;
  imageId: string;
  storeKey: string;
  imagePropertyIds: string[];
}

export interface ImageServiceAddImageRequest {
  /** The protocol version id to add this image to. */
  protocolVersionId: string;
  /** Whether this is a public or org-image version. */
  orgId?:
    | string
    | undefined;
  /** An identifier to where the image is stored. */
  imageUri: string;
  /** A readable description of this image. */
  description?:
    | string
    | undefined;
  /** The set of properties for configuring this image. */
  properties: AddImageProperty[];
  /** The firewall config for this image. */
  firewall:
    | FirewallConfig
    | undefined;
  /** The minimum CPU cores to run this image. */
  minCpuCores: number;
  /** The minimum memory to run this image. */
  minMemoryBytes: number;
  /** The minimum disk space to run this image. */
  minDiskBytes: number;
  /** The minimum babel version to run this image. */
  minBabelVersion: string;
  /** The set of attached ramdisks. */
  ramdisks: RamdiskConfig[];
  /** Point each combination of new_archive properties to the correct data. */
  archivePointers: ArchivePointer[];
  /** The DNS scheme (e.g. "https", "wss") for node RPC requests. */
  dnsScheme?: string | undefined;
}

export interface AddImageProperty {
  /** The lookup key for this property (in lower-kebab-case). */
  key: string;
  /** A repeated key to group switches and enums. */
  keyGroup?:
    | string
    | undefined;
  /** For repeated key groups, one value must be the default. */
  isGroupDefault?:
    | boolean
    | undefined;
  /** Whether this property requires different archive data. */
  newArchive: boolean;
  /** The default value for this property. */
  defaultValue: string;
  /** Whether the value may change at runtime. */
  dynamicValue: boolean;
  /** How this field should be displayed in the UI. */
  uiType: UiType;
  /** The display name of this property. */
  displayName?:
    | string
    | undefined;
  /** The display name of the property group. */
  displayGroup?:
    | string
    | undefined;
  /** Additional information describing this property. */
  description?:
    | string
    | undefined;
  /** Additional CPU cores needed when using this property. */
  addCpuCores?:
    | number
    | undefined;
  /** Additional memory bytes needed when using this property. */
  addMemoryBytes?:
    | number
    | undefined;
  /** Additional disk bytes needed when using this property. */
  addDiskBytes?: number | undefined;
}

export interface ArchivePointer {
  /** The combination of new_archive property keys to lookup an archive. */
  newArchiveKeys: string[];
  /** Use this store key. */
  storeKey?:
    | string
    | undefined;
  /** This combination of new_archive properties is disallowed. */
  disallowed?: Empty | undefined;
}

export interface ImageServiceAddImageResponse {
  image: Image | undefined;
  archives: Archive[];
}

export interface ImageServiceGetImageRequest {
  /** The version key to get the latest image of. */
  versionKey:
    | ProtocolVersionKey
    | undefined;
  /** The org id for private protocols, versions or images. */
  orgId?:
    | string
    | undefined;
  /** A semantic protocol version, or none for latest. */
  semanticVersion?:
    | string
    | undefined;
  /** An image build version, or none for latest. */
  buildVersion?: number | undefined;
}

export interface ImageServiceGetImageResponse {
  image: Image | undefined;
}

export interface ImageServiceListArchivesRequest {
  imageId: string;
  orgId?: string | undefined;
}

export interface ImageServiceListArchivesResponse {
  archives: Archive[];
}

export interface ImageServiceUpdateArchiveRequest {
  archiveId: string;
  storeKey?: string | undefined;
}

export interface ImageServiceUpdateArchiveResponse {
  archive: Archive | undefined;
}

export interface ImageServiceUpdateImageRequest {
  imageId: string;
  visibility?: Visibility | undefined;
}

export interface ImageServiceUpdateImageResponse {
  image: Image | undefined;
}

/** Summary information about an image for admin list views. */
export interface ImageSummary {
  imageId: string;
  protocolName: string;
  versionKey: ProtocolVersionKey | undefined;
  buildVersion: number;
  propertyCount: number;
  createdAt: Date | undefined;
  orgId?: string | undefined;
  description?: string | undefined;
  visibility: Visibility;
  semanticVersion: string;
}

/** Request message for listing images with filtering and pagination. */
export interface ImageServiceListImagesRequest {
  search?: string | undefined;
  protocolFilter?: string | undefined;
  orgFilter?: string | undefined;
  page: number;
  pageSize: number;
}

/** Response message for listing images. */
export interface ImageServiceListImagesResponse {
  images: ImageSummary[];
  totalCount: number;
}

/** Request message for getting detailed image information. */
export interface ImageServiceGetImageDetailsRequest {
  imageId: string;
  orgId?: string | undefined;
}

/** Response message for getting detailed image information. */
export interface ImageServiceGetImageDetailsResponse {
  image: Image | undefined;
}

/** Request message for adding a new image property. */
export interface ImageServiceAddImagePropertyRequest {
  imageId: string;
  property: AddImageProperty | undefined;
  applyToNewerVersions: boolean;
}

/** Response message for adding a new image property. */
export interface ImageServiceAddImagePropertyResponse {
  property: ImageProperty | undefined;
  affectedImageIds: string[];
}

/** Request message for updating an existing image property. */
export interface ImageServiceUpdateImagePropertyRequest {
  imagePropertyId: string;
  property: AddImageProperty | undefined;
  applyToNewerVersions: boolean;
}

/** Response message for updating an existing image property. */
export interface ImageServiceUpdateImagePropertyResponse {
  property: ImageProperty | undefined;
  affectedImageIds: string[];
}

/** Request message for deleting an image property. */
export interface ImageServiceDeleteImagePropertyRequest {
  imagePropertyId: string;
  applyToNewerVersions: boolean;
}

/** Response message for deleting an image property. */
export interface ImageServiceDeleteImagePropertyResponse {
  affectedImageIds: string[];
}

/** Request message for copying properties between image versions. */
export interface ImageServiceCopyImagePropertiesRequest {
  sourceImageId: string;
  targetImageIds: string[];
  overwriteExisting?: boolean | undefined;
}

/** Response message for copying properties between image versions. */
export interface ImageServiceCopyImagePropertiesResponse {
  affectedImageIds: string[];
  propertiesCopied: number;
}

function createBaseImage(): Image {
  return {
    imageId: "",
    protocolVersionId: "",
    orgId: undefined,
    buildVersion: 0,
    imageUri: "",
    description: undefined,
    properties: [],
    firewall: undefined,
    minCpuCores: 0,
    minMemoryBytes: 0,
    minDiskBytes: 0,
    minBabelVersion: "",
    ramdisks: [],
    visibility: 0,
    createdAt: undefined,
    updatedAt: undefined,
    dnsScheme: undefined,
  };
}

export const Image: MessageFns<Image> = {
  encode(message: Image, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageId !== "") {
      writer.uint32(10).string(message.imageId);
    }
    if (message.protocolVersionId !== "") {
      writer.uint32(18).string(message.protocolVersionId);
    }
    if (message.orgId !== undefined) {
      writer.uint32(26).string(message.orgId);
    }
    if (message.buildVersion !== 0) {
      writer.uint32(32).uint64(message.buildVersion);
    }
    if (message.imageUri !== "") {
      writer.uint32(42).string(message.imageUri);
    }
    if (message.description !== undefined) {
      writer.uint32(50).string(message.description);
    }
    for (const v of message.properties) {
      ImageProperty.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.firewall !== undefined) {
      FirewallConfig.encode(message.firewall, writer.uint32(66).fork()).join();
    }
    if (message.minCpuCores !== 0) {
      writer.uint32(72).uint64(message.minCpuCores);
    }
    if (message.minMemoryBytes !== 0) {
      writer.uint32(80).uint64(message.minMemoryBytes);
    }
    if (message.minDiskBytes !== 0) {
      writer.uint32(88).uint64(message.minDiskBytes);
    }
    if (message.minBabelVersion !== "") {
      writer.uint32(98).string(message.minBabelVersion);
    }
    for (const v of message.ramdisks) {
      RamdiskConfig.encode(v!, writer.uint32(106).fork()).join();
    }
    if (message.visibility !== 0) {
      writer.uint32(112).int32(message.visibility);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(122).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(130).fork()).join();
    }
    if (message.dnsScheme !== undefined) {
      writer.uint32(138).string(message.dnsScheme);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Image {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.protocolVersionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.buildVersion = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.imageUri = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.properties.push(ImageProperty.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.firewall = FirewallConfig.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.minCpuCores = longToNumber(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.minMemoryBytes = longToNumber(reader.uint64());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.minDiskBytes = longToNumber(reader.uint64());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.minBabelVersion = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.ramdisks.push(RamdiskConfig.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.visibility = reader.int32() as any;
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.dnsScheme = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Image>): Image {
    return Image.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Image>): Image {
    const message = createBaseImage();
    message.imageId = object.imageId ?? "";
    message.protocolVersionId = object.protocolVersionId ?? "";
    message.orgId = object.orgId ?? undefined;
    message.buildVersion = object.buildVersion ?? 0;
    message.imageUri = object.imageUri ?? "";
    message.description = object.description ?? undefined;
    message.properties = object.properties?.map((e) => ImageProperty.fromPartial(e)) || [];
    message.firewall = (object.firewall !== undefined && object.firewall !== null)
      ? FirewallConfig.fromPartial(object.firewall)
      : undefined;
    message.minCpuCores = object.minCpuCores ?? 0;
    message.minMemoryBytes = object.minMemoryBytes ?? 0;
    message.minDiskBytes = object.minDiskBytes ?? 0;
    message.minBabelVersion = object.minBabelVersion ?? "";
    message.ramdisks = object.ramdisks?.map((e) => RamdiskConfig.fromPartial(e)) || [];
    message.visibility = object.visibility ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.dnsScheme = object.dnsScheme ?? undefined;
    return message;
  },
};

function createBaseImageProperty(): ImageProperty {
  return {
    imagePropertyId: "",
    imageId: "",
    key: "",
    keyGroup: undefined,
    isGroupDefault: undefined,
    newArchive: false,
    defaultValue: "",
    dynamicValue: false,
    uiType: 0,
    displayName: undefined,
    displayGroup: undefined,
    description: undefined,
    addCpuCores: undefined,
    addMemoryBytes: undefined,
    addDiskBytes: undefined,
  };
}

export const ImageProperty: MessageFns<ImageProperty> = {
  encode(message: ImageProperty, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imagePropertyId !== "") {
      writer.uint32(10).string(message.imagePropertyId);
    }
    if (message.imageId !== "") {
      writer.uint32(18).string(message.imageId);
    }
    if (message.key !== "") {
      writer.uint32(26).string(message.key);
    }
    if (message.keyGroup !== undefined) {
      writer.uint32(34).string(message.keyGroup);
    }
    if (message.isGroupDefault !== undefined) {
      writer.uint32(40).bool(message.isGroupDefault);
    }
    if (message.newArchive !== false) {
      writer.uint32(48).bool(message.newArchive);
    }
    if (message.defaultValue !== "") {
      writer.uint32(58).string(message.defaultValue);
    }
    if (message.dynamicValue !== false) {
      writer.uint32(64).bool(message.dynamicValue);
    }
    if (message.uiType !== 0) {
      writer.uint32(72).int32(message.uiType);
    }
    if (message.displayName !== undefined) {
      writer.uint32(82).string(message.displayName);
    }
    if (message.displayGroup !== undefined) {
      writer.uint32(90).string(message.displayGroup);
    }
    if (message.description !== undefined) {
      writer.uint32(98).string(message.description);
    }
    if (message.addCpuCores !== undefined) {
      writer.uint32(104).int64(message.addCpuCores);
    }
    if (message.addMemoryBytes !== undefined) {
      writer.uint32(112).int64(message.addMemoryBytes);
    }
    if (message.addDiskBytes !== undefined) {
      writer.uint32(120).int64(message.addDiskBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageProperty {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageProperty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imagePropertyId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.keyGroup = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isGroupDefault = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.newArchive = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.defaultValue = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.dynamicValue = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.uiType = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.displayGroup = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.addCpuCores = longToNumber(reader.int64());
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.addMemoryBytes = longToNumber(reader.int64());
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.addDiskBytes = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageProperty>): ImageProperty {
    return ImageProperty.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageProperty>): ImageProperty {
    const message = createBaseImageProperty();
    message.imagePropertyId = object.imagePropertyId ?? "";
    message.imageId = object.imageId ?? "";
    message.key = object.key ?? "";
    message.keyGroup = object.keyGroup ?? undefined;
    message.isGroupDefault = object.isGroupDefault ?? undefined;
    message.newArchive = object.newArchive ?? false;
    message.defaultValue = object.defaultValue ?? "";
    message.dynamicValue = object.dynamicValue ?? false;
    message.uiType = object.uiType ?? 0;
    message.displayName = object.displayName ?? undefined;
    message.displayGroup = object.displayGroup ?? undefined;
    message.description = object.description ?? undefined;
    message.addCpuCores = object.addCpuCores ?? undefined;
    message.addMemoryBytes = object.addMemoryBytes ?? undefined;
    message.addDiskBytes = object.addDiskBytes ?? undefined;
    return message;
  },
};

function createBaseArchive(): Archive {
  return { archiveId: "", imageId: "", storeKey: "", imagePropertyIds: [] };
}

export const Archive: MessageFns<Archive> = {
  encode(message: Archive, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.archiveId !== "") {
      writer.uint32(10).string(message.archiveId);
    }
    if (message.imageId !== "") {
      writer.uint32(18).string(message.imageId);
    }
    if (message.storeKey !== "") {
      writer.uint32(26).string(message.storeKey);
    }
    for (const v of message.imagePropertyIds) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Archive {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchive();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.archiveId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storeKey = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.imagePropertyIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Archive>): Archive {
    return Archive.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Archive>): Archive {
    const message = createBaseArchive();
    message.archiveId = object.archiveId ?? "";
    message.imageId = object.imageId ?? "";
    message.storeKey = object.storeKey ?? "";
    message.imagePropertyIds = object.imagePropertyIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseImageServiceAddImageRequest(): ImageServiceAddImageRequest {
  return {
    protocolVersionId: "",
    orgId: undefined,
    imageUri: "",
    description: undefined,
    properties: [],
    firewall: undefined,
    minCpuCores: 0,
    minMemoryBytes: 0,
    minDiskBytes: 0,
    minBabelVersion: "",
    ramdisks: [],
    archivePointers: [],
    dnsScheme: undefined,
  };
}

export const ImageServiceAddImageRequest: MessageFns<ImageServiceAddImageRequest> = {
  encode(message: ImageServiceAddImageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolVersionId !== "") {
      writer.uint32(10).string(message.protocolVersionId);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    if (message.imageUri !== "") {
      writer.uint32(26).string(message.imageUri);
    }
    if (message.description !== undefined) {
      writer.uint32(34).string(message.description);
    }
    for (const v of message.properties) {
      AddImageProperty.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.firewall !== undefined) {
      FirewallConfig.encode(message.firewall, writer.uint32(50).fork()).join();
    }
    if (message.minCpuCores !== 0) {
      writer.uint32(56).uint64(message.minCpuCores);
    }
    if (message.minMemoryBytes !== 0) {
      writer.uint32(64).uint64(message.minMemoryBytes);
    }
    if (message.minDiskBytes !== 0) {
      writer.uint32(72).uint64(message.minDiskBytes);
    }
    if (message.minBabelVersion !== "") {
      writer.uint32(82).string(message.minBabelVersion);
    }
    for (const v of message.ramdisks) {
      RamdiskConfig.encode(v!, writer.uint32(90).fork()).join();
    }
    for (const v of message.archivePointers) {
      ArchivePointer.encode(v!, writer.uint32(98).fork()).join();
    }
    if (message.dnsScheme !== undefined) {
      writer.uint32(106).string(message.dnsScheme);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceAddImageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceAddImageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.protocolVersionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.imageUri = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.properties.push(AddImageProperty.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.firewall = FirewallConfig.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.minCpuCores = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.minMemoryBytes = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.minDiskBytes = longToNumber(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.minBabelVersion = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.ramdisks.push(RamdiskConfig.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.archivePointers.push(ArchivePointer.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.dnsScheme = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceAddImageRequest>): ImageServiceAddImageRequest {
    return ImageServiceAddImageRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceAddImageRequest>): ImageServiceAddImageRequest {
    const message = createBaseImageServiceAddImageRequest();
    message.protocolVersionId = object.protocolVersionId ?? "";
    message.orgId = object.orgId ?? undefined;
    message.imageUri = object.imageUri ?? "";
    message.description = object.description ?? undefined;
    message.properties = object.properties?.map((e) => AddImageProperty.fromPartial(e)) || [];
    message.firewall = (object.firewall !== undefined && object.firewall !== null)
      ? FirewallConfig.fromPartial(object.firewall)
      : undefined;
    message.minCpuCores = object.minCpuCores ?? 0;
    message.minMemoryBytes = object.minMemoryBytes ?? 0;
    message.minDiskBytes = object.minDiskBytes ?? 0;
    message.minBabelVersion = object.minBabelVersion ?? "";
    message.ramdisks = object.ramdisks?.map((e) => RamdiskConfig.fromPartial(e)) || [];
    message.archivePointers = object.archivePointers?.map((e) => ArchivePointer.fromPartial(e)) || [];
    message.dnsScheme = object.dnsScheme ?? undefined;
    return message;
  },
};

function createBaseAddImageProperty(): AddImageProperty {
  return {
    key: "",
    keyGroup: undefined,
    isGroupDefault: undefined,
    newArchive: false,
    defaultValue: "",
    dynamicValue: false,
    uiType: 0,
    displayName: undefined,
    displayGroup: undefined,
    description: undefined,
    addCpuCores: undefined,
    addMemoryBytes: undefined,
    addDiskBytes: undefined,
  };
}

export const AddImageProperty: MessageFns<AddImageProperty> = {
  encode(message: AddImageProperty, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.keyGroup !== undefined) {
      writer.uint32(18).string(message.keyGroup);
    }
    if (message.isGroupDefault !== undefined) {
      writer.uint32(24).bool(message.isGroupDefault);
    }
    if (message.newArchive !== false) {
      writer.uint32(32).bool(message.newArchive);
    }
    if (message.defaultValue !== "") {
      writer.uint32(42).string(message.defaultValue);
    }
    if (message.dynamicValue !== false) {
      writer.uint32(48).bool(message.dynamicValue);
    }
    if (message.uiType !== 0) {
      writer.uint32(56).int32(message.uiType);
    }
    if (message.displayName !== undefined) {
      writer.uint32(66).string(message.displayName);
    }
    if (message.displayGroup !== undefined) {
      writer.uint32(74).string(message.displayGroup);
    }
    if (message.description !== undefined) {
      writer.uint32(82).string(message.description);
    }
    if (message.addCpuCores !== undefined) {
      writer.uint32(88).int64(message.addCpuCores);
    }
    if (message.addMemoryBytes !== undefined) {
      writer.uint32(96).int64(message.addMemoryBytes);
    }
    if (message.addDiskBytes !== undefined) {
      writer.uint32(104).int64(message.addDiskBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddImageProperty {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddImageProperty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.keyGroup = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isGroupDefault = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.newArchive = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.defaultValue = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.dynamicValue = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.uiType = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.displayGroup = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.addCpuCores = longToNumber(reader.int64());
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.addMemoryBytes = longToNumber(reader.int64());
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.addDiskBytes = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<AddImageProperty>): AddImageProperty {
    return AddImageProperty.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AddImageProperty>): AddImageProperty {
    const message = createBaseAddImageProperty();
    message.key = object.key ?? "";
    message.keyGroup = object.keyGroup ?? undefined;
    message.isGroupDefault = object.isGroupDefault ?? undefined;
    message.newArchive = object.newArchive ?? false;
    message.defaultValue = object.defaultValue ?? "";
    message.dynamicValue = object.dynamicValue ?? false;
    message.uiType = object.uiType ?? 0;
    message.displayName = object.displayName ?? undefined;
    message.displayGroup = object.displayGroup ?? undefined;
    message.description = object.description ?? undefined;
    message.addCpuCores = object.addCpuCores ?? undefined;
    message.addMemoryBytes = object.addMemoryBytes ?? undefined;
    message.addDiskBytes = object.addDiskBytes ?? undefined;
    return message;
  },
};

function createBaseArchivePointer(): ArchivePointer {
  return { newArchiveKeys: [], storeKey: undefined, disallowed: undefined };
}

export const ArchivePointer: MessageFns<ArchivePointer> = {
  encode(message: ArchivePointer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.newArchiveKeys) {
      writer.uint32(10).string(v!);
    }
    if (message.storeKey !== undefined) {
      writer.uint32(26).string(message.storeKey);
    }
    if (message.disallowed !== undefined) {
      Empty.encode(message.disallowed, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchivePointer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchivePointer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.newArchiveKeys.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storeKey = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.disallowed = Empty.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ArchivePointer>): ArchivePointer {
    return ArchivePointer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchivePointer>): ArchivePointer {
    const message = createBaseArchivePointer();
    message.newArchiveKeys = object.newArchiveKeys?.map((e) => e) || [];
    message.storeKey = object.storeKey ?? undefined;
    message.disallowed = (object.disallowed !== undefined && object.disallowed !== null)
      ? Empty.fromPartial(object.disallowed)
      : undefined;
    return message;
  },
};

function createBaseImageServiceAddImageResponse(): ImageServiceAddImageResponse {
  return { image: undefined, archives: [] };
}

export const ImageServiceAddImageResponse: MessageFns<ImageServiceAddImageResponse> = {
  encode(message: ImageServiceAddImageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      Image.encode(message.image, writer.uint32(10).fork()).join();
    }
    for (const v of message.archives) {
      Archive.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceAddImageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceAddImageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = Image.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.archives.push(Archive.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceAddImageResponse>): ImageServiceAddImageResponse {
    return ImageServiceAddImageResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceAddImageResponse>): ImageServiceAddImageResponse {
    const message = createBaseImageServiceAddImageResponse();
    message.image = (object.image !== undefined && object.image !== null) ? Image.fromPartial(object.image) : undefined;
    message.archives = object.archives?.map((e) => Archive.fromPartial(e)) || [];
    return message;
  },
};

function createBaseImageServiceGetImageRequest(): ImageServiceGetImageRequest {
  return { versionKey: undefined, orgId: undefined, semanticVersion: undefined, buildVersion: undefined };
}

export const ImageServiceGetImageRequest: MessageFns<ImageServiceGetImageRequest> = {
  encode(message: ImageServiceGetImageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.versionKey !== undefined) {
      ProtocolVersionKey.encode(message.versionKey, writer.uint32(10).fork()).join();
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    if (message.semanticVersion !== undefined) {
      writer.uint32(26).string(message.semanticVersion);
    }
    if (message.buildVersion !== undefined) {
      writer.uint32(32).uint64(message.buildVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceGetImageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceGetImageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.versionKey = ProtocolVersionKey.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.semanticVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.buildVersion = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceGetImageRequest>): ImageServiceGetImageRequest {
    return ImageServiceGetImageRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceGetImageRequest>): ImageServiceGetImageRequest {
    const message = createBaseImageServiceGetImageRequest();
    message.versionKey = (object.versionKey !== undefined && object.versionKey !== null)
      ? ProtocolVersionKey.fromPartial(object.versionKey)
      : undefined;
    message.orgId = object.orgId ?? undefined;
    message.semanticVersion = object.semanticVersion ?? undefined;
    message.buildVersion = object.buildVersion ?? undefined;
    return message;
  },
};

function createBaseImageServiceGetImageResponse(): ImageServiceGetImageResponse {
  return { image: undefined };
}

export const ImageServiceGetImageResponse: MessageFns<ImageServiceGetImageResponse> = {
  encode(message: ImageServiceGetImageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      Image.encode(message.image, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceGetImageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceGetImageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = Image.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceGetImageResponse>): ImageServiceGetImageResponse {
    return ImageServiceGetImageResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceGetImageResponse>): ImageServiceGetImageResponse {
    const message = createBaseImageServiceGetImageResponse();
    message.image = (object.image !== undefined && object.image !== null) ? Image.fromPartial(object.image) : undefined;
    return message;
  },
};

function createBaseImageServiceListArchivesRequest(): ImageServiceListArchivesRequest {
  return { imageId: "", orgId: undefined };
}

export const ImageServiceListArchivesRequest: MessageFns<ImageServiceListArchivesRequest> = {
  encode(message: ImageServiceListArchivesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageId !== "") {
      writer.uint32(10).string(message.imageId);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceListArchivesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceListArchivesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceListArchivesRequest>): ImageServiceListArchivesRequest {
    return ImageServiceListArchivesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceListArchivesRequest>): ImageServiceListArchivesRequest {
    const message = createBaseImageServiceListArchivesRequest();
    message.imageId = object.imageId ?? "";
    message.orgId = object.orgId ?? undefined;
    return message;
  },
};

function createBaseImageServiceListArchivesResponse(): ImageServiceListArchivesResponse {
  return { archives: [] };
}

export const ImageServiceListArchivesResponse: MessageFns<ImageServiceListArchivesResponse> = {
  encode(message: ImageServiceListArchivesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.archives) {
      Archive.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceListArchivesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceListArchivesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.archives.push(Archive.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceListArchivesResponse>): ImageServiceListArchivesResponse {
    return ImageServiceListArchivesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceListArchivesResponse>): ImageServiceListArchivesResponse {
    const message = createBaseImageServiceListArchivesResponse();
    message.archives = object.archives?.map((e) => Archive.fromPartial(e)) || [];
    return message;
  },
};

function createBaseImageServiceUpdateArchiveRequest(): ImageServiceUpdateArchiveRequest {
  return { archiveId: "", storeKey: undefined };
}

export const ImageServiceUpdateArchiveRequest: MessageFns<ImageServiceUpdateArchiveRequest> = {
  encode(message: ImageServiceUpdateArchiveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.archiveId !== "") {
      writer.uint32(10).string(message.archiveId);
    }
    if (message.storeKey !== undefined) {
      writer.uint32(18).string(message.storeKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceUpdateArchiveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceUpdateArchiveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.archiveId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.storeKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceUpdateArchiveRequest>): ImageServiceUpdateArchiveRequest {
    return ImageServiceUpdateArchiveRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceUpdateArchiveRequest>): ImageServiceUpdateArchiveRequest {
    const message = createBaseImageServiceUpdateArchiveRequest();
    message.archiveId = object.archiveId ?? "";
    message.storeKey = object.storeKey ?? undefined;
    return message;
  },
};

function createBaseImageServiceUpdateArchiveResponse(): ImageServiceUpdateArchiveResponse {
  return { archive: undefined };
}

export const ImageServiceUpdateArchiveResponse: MessageFns<ImageServiceUpdateArchiveResponse> = {
  encode(message: ImageServiceUpdateArchiveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.archive !== undefined) {
      Archive.encode(message.archive, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceUpdateArchiveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceUpdateArchiveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.archive = Archive.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceUpdateArchiveResponse>): ImageServiceUpdateArchiveResponse {
    return ImageServiceUpdateArchiveResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceUpdateArchiveResponse>): ImageServiceUpdateArchiveResponse {
    const message = createBaseImageServiceUpdateArchiveResponse();
    message.archive = (object.archive !== undefined && object.archive !== null)
      ? Archive.fromPartial(object.archive)
      : undefined;
    return message;
  },
};

function createBaseImageServiceUpdateImageRequest(): ImageServiceUpdateImageRequest {
  return { imageId: "", visibility: undefined };
}

export const ImageServiceUpdateImageRequest: MessageFns<ImageServiceUpdateImageRequest> = {
  encode(message: ImageServiceUpdateImageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageId !== "") {
      writer.uint32(10).string(message.imageId);
    }
    if (message.visibility !== undefined) {
      writer.uint32(16).int32(message.visibility);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceUpdateImageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceUpdateImageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.visibility = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceUpdateImageRequest>): ImageServiceUpdateImageRequest {
    return ImageServiceUpdateImageRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceUpdateImageRequest>): ImageServiceUpdateImageRequest {
    const message = createBaseImageServiceUpdateImageRequest();
    message.imageId = object.imageId ?? "";
    message.visibility = object.visibility ?? undefined;
    return message;
  },
};

function createBaseImageServiceUpdateImageResponse(): ImageServiceUpdateImageResponse {
  return { image: undefined };
}

export const ImageServiceUpdateImageResponse: MessageFns<ImageServiceUpdateImageResponse> = {
  encode(message: ImageServiceUpdateImageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      Image.encode(message.image, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceUpdateImageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceUpdateImageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = Image.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceUpdateImageResponse>): ImageServiceUpdateImageResponse {
    return ImageServiceUpdateImageResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceUpdateImageResponse>): ImageServiceUpdateImageResponse {
    const message = createBaseImageServiceUpdateImageResponse();
    message.image = (object.image !== undefined && object.image !== null) ? Image.fromPartial(object.image) : undefined;
    return message;
  },
};

function createBaseImageSummary(): ImageSummary {
  return {
    imageId: "",
    protocolName: "",
    versionKey: undefined,
    buildVersion: 0,
    propertyCount: 0,
    createdAt: undefined,
    orgId: undefined,
    description: undefined,
    visibility: 0,
    semanticVersion: "",
  };
}

export const ImageSummary: MessageFns<ImageSummary> = {
  encode(message: ImageSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageId !== "") {
      writer.uint32(10).string(message.imageId);
    }
    if (message.protocolName !== "") {
      writer.uint32(18).string(message.protocolName);
    }
    if (message.versionKey !== undefined) {
      ProtocolVersionKey.encode(message.versionKey, writer.uint32(26).fork()).join();
    }
    if (message.buildVersion !== 0) {
      writer.uint32(32).uint64(message.buildVersion);
    }
    if (message.propertyCount !== 0) {
      writer.uint32(40).uint32(message.propertyCount);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(50).fork()).join();
    }
    if (message.orgId !== undefined) {
      writer.uint32(58).string(message.orgId);
    }
    if (message.description !== undefined) {
      writer.uint32(66).string(message.description);
    }
    if (message.visibility !== 0) {
      writer.uint32(72).int32(message.visibility);
    }
    if (message.semanticVersion !== "") {
      writer.uint32(82).string(message.semanticVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.protocolName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.versionKey = ProtocolVersionKey.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.buildVersion = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.propertyCount = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.visibility = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.semanticVersion = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageSummary>): ImageSummary {
    return ImageSummary.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageSummary>): ImageSummary {
    const message = createBaseImageSummary();
    message.imageId = object.imageId ?? "";
    message.protocolName = object.protocolName ?? "";
    message.versionKey = (object.versionKey !== undefined && object.versionKey !== null)
      ? ProtocolVersionKey.fromPartial(object.versionKey)
      : undefined;
    message.buildVersion = object.buildVersion ?? 0;
    message.propertyCount = object.propertyCount ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.orgId = object.orgId ?? undefined;
    message.description = object.description ?? undefined;
    message.visibility = object.visibility ?? 0;
    message.semanticVersion = object.semanticVersion ?? "";
    return message;
  },
};

function createBaseImageServiceListImagesRequest(): ImageServiceListImagesRequest {
  return { search: undefined, protocolFilter: undefined, orgFilter: undefined, page: 0, pageSize: 0 };
}

export const ImageServiceListImagesRequest: MessageFns<ImageServiceListImagesRequest> = {
  encode(message: ImageServiceListImagesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.search !== undefined) {
      writer.uint32(10).string(message.search);
    }
    if (message.protocolFilter !== undefined) {
      writer.uint32(18).string(message.protocolFilter);
    }
    if (message.orgFilter !== undefined) {
      writer.uint32(26).string(message.orgFilter);
    }
    if (message.page !== 0) {
      writer.uint32(32).uint32(message.page);
    }
    if (message.pageSize !== 0) {
      writer.uint32(40).uint32(message.pageSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceListImagesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceListImagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.search = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.protocolFilter = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orgFilter = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.page = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.pageSize = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceListImagesRequest>): ImageServiceListImagesRequest {
    return ImageServiceListImagesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceListImagesRequest>): ImageServiceListImagesRequest {
    const message = createBaseImageServiceListImagesRequest();
    message.search = object.search ?? undefined;
    message.protocolFilter = object.protocolFilter ?? undefined;
    message.orgFilter = object.orgFilter ?? undefined;
    message.page = object.page ?? 0;
    message.pageSize = object.pageSize ?? 0;
    return message;
  },
};

function createBaseImageServiceListImagesResponse(): ImageServiceListImagesResponse {
  return { images: [], totalCount: 0 };
}

export const ImageServiceListImagesResponse: MessageFns<ImageServiceListImagesResponse> = {
  encode(message: ImageServiceListImagesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.images) {
      ImageSummary.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalCount !== 0) {
      writer.uint32(16).uint32(message.totalCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceListImagesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceListImagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.images.push(ImageSummary.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceListImagesResponse>): ImageServiceListImagesResponse {
    return ImageServiceListImagesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceListImagesResponse>): ImageServiceListImagesResponse {
    const message = createBaseImageServiceListImagesResponse();
    message.images = object.images?.map((e) => ImageSummary.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0;
    return message;
  },
};

function createBaseImageServiceGetImageDetailsRequest(): ImageServiceGetImageDetailsRequest {
  return { imageId: "", orgId: undefined };
}

export const ImageServiceGetImageDetailsRequest: MessageFns<ImageServiceGetImageDetailsRequest> = {
  encode(message: ImageServiceGetImageDetailsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageId !== "") {
      writer.uint32(10).string(message.imageId);
    }
    if (message.orgId !== undefined) {
      writer.uint32(18).string(message.orgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceGetImageDetailsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceGetImageDetailsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orgId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceGetImageDetailsRequest>): ImageServiceGetImageDetailsRequest {
    return ImageServiceGetImageDetailsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceGetImageDetailsRequest>): ImageServiceGetImageDetailsRequest {
    const message = createBaseImageServiceGetImageDetailsRequest();
    message.imageId = object.imageId ?? "";
    message.orgId = object.orgId ?? undefined;
    return message;
  },
};

function createBaseImageServiceGetImageDetailsResponse(): ImageServiceGetImageDetailsResponse {
  return { image: undefined };
}

export const ImageServiceGetImageDetailsResponse: MessageFns<ImageServiceGetImageDetailsResponse> = {
  encode(message: ImageServiceGetImageDetailsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== undefined) {
      Image.encode(message.image, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceGetImageDetailsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceGetImageDetailsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = Image.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceGetImageDetailsResponse>): ImageServiceGetImageDetailsResponse {
    return ImageServiceGetImageDetailsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceGetImageDetailsResponse>): ImageServiceGetImageDetailsResponse {
    const message = createBaseImageServiceGetImageDetailsResponse();
    message.image = (object.image !== undefined && object.image !== null) ? Image.fromPartial(object.image) : undefined;
    return message;
  },
};

function createBaseImageServiceAddImagePropertyRequest(): ImageServiceAddImagePropertyRequest {
  return { imageId: "", property: undefined, applyToNewerVersions: false };
}

export const ImageServiceAddImagePropertyRequest: MessageFns<ImageServiceAddImagePropertyRequest> = {
  encode(message: ImageServiceAddImagePropertyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageId !== "") {
      writer.uint32(10).string(message.imageId);
    }
    if (message.property !== undefined) {
      AddImageProperty.encode(message.property, writer.uint32(18).fork()).join();
    }
    if (message.applyToNewerVersions !== false) {
      writer.uint32(24).bool(message.applyToNewerVersions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceAddImagePropertyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceAddImagePropertyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.property = AddImageProperty.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.applyToNewerVersions = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceAddImagePropertyRequest>): ImageServiceAddImagePropertyRequest {
    return ImageServiceAddImagePropertyRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceAddImagePropertyRequest>): ImageServiceAddImagePropertyRequest {
    const message = createBaseImageServiceAddImagePropertyRequest();
    message.imageId = object.imageId ?? "";
    message.property = (object.property !== undefined && object.property !== null)
      ? AddImageProperty.fromPartial(object.property)
      : undefined;
    message.applyToNewerVersions = object.applyToNewerVersions ?? false;
    return message;
  },
};

function createBaseImageServiceAddImagePropertyResponse(): ImageServiceAddImagePropertyResponse {
  return { property: undefined, affectedImageIds: [] };
}

export const ImageServiceAddImagePropertyResponse: MessageFns<ImageServiceAddImagePropertyResponse> = {
  encode(message: ImageServiceAddImagePropertyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.property !== undefined) {
      ImageProperty.encode(message.property, writer.uint32(10).fork()).join();
    }
    for (const v of message.affectedImageIds) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceAddImagePropertyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceAddImagePropertyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.property = ImageProperty.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.affectedImageIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceAddImagePropertyResponse>): ImageServiceAddImagePropertyResponse {
    return ImageServiceAddImagePropertyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceAddImagePropertyResponse>): ImageServiceAddImagePropertyResponse {
    const message = createBaseImageServiceAddImagePropertyResponse();
    message.property = (object.property !== undefined && object.property !== null)
      ? ImageProperty.fromPartial(object.property)
      : undefined;
    message.affectedImageIds = object.affectedImageIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseImageServiceUpdateImagePropertyRequest(): ImageServiceUpdateImagePropertyRequest {
  return { imagePropertyId: "", property: undefined, applyToNewerVersions: false };
}

export const ImageServiceUpdateImagePropertyRequest: MessageFns<ImageServiceUpdateImagePropertyRequest> = {
  encode(message: ImageServiceUpdateImagePropertyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imagePropertyId !== "") {
      writer.uint32(10).string(message.imagePropertyId);
    }
    if (message.property !== undefined) {
      AddImageProperty.encode(message.property, writer.uint32(18).fork()).join();
    }
    if (message.applyToNewerVersions !== false) {
      writer.uint32(24).bool(message.applyToNewerVersions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceUpdateImagePropertyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceUpdateImagePropertyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imagePropertyId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.property = AddImageProperty.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.applyToNewerVersions = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceUpdateImagePropertyRequest>): ImageServiceUpdateImagePropertyRequest {
    return ImageServiceUpdateImagePropertyRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceUpdateImagePropertyRequest>): ImageServiceUpdateImagePropertyRequest {
    const message = createBaseImageServiceUpdateImagePropertyRequest();
    message.imagePropertyId = object.imagePropertyId ?? "";
    message.property = (object.property !== undefined && object.property !== null)
      ? AddImageProperty.fromPartial(object.property)
      : undefined;
    message.applyToNewerVersions = object.applyToNewerVersions ?? false;
    return message;
  },
};

function createBaseImageServiceUpdateImagePropertyResponse(): ImageServiceUpdateImagePropertyResponse {
  return { property: undefined, affectedImageIds: [] };
}

export const ImageServiceUpdateImagePropertyResponse: MessageFns<ImageServiceUpdateImagePropertyResponse> = {
  encode(message: ImageServiceUpdateImagePropertyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.property !== undefined) {
      ImageProperty.encode(message.property, writer.uint32(10).fork()).join();
    }
    for (const v of message.affectedImageIds) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceUpdateImagePropertyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceUpdateImagePropertyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.property = ImageProperty.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.affectedImageIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceUpdateImagePropertyResponse>): ImageServiceUpdateImagePropertyResponse {
    return ImageServiceUpdateImagePropertyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceUpdateImagePropertyResponse>): ImageServiceUpdateImagePropertyResponse {
    const message = createBaseImageServiceUpdateImagePropertyResponse();
    message.property = (object.property !== undefined && object.property !== null)
      ? ImageProperty.fromPartial(object.property)
      : undefined;
    message.affectedImageIds = object.affectedImageIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseImageServiceDeleteImagePropertyRequest(): ImageServiceDeleteImagePropertyRequest {
  return { imagePropertyId: "", applyToNewerVersions: false };
}

export const ImageServiceDeleteImagePropertyRequest: MessageFns<ImageServiceDeleteImagePropertyRequest> = {
  encode(message: ImageServiceDeleteImagePropertyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imagePropertyId !== "") {
      writer.uint32(10).string(message.imagePropertyId);
    }
    if (message.applyToNewerVersions !== false) {
      writer.uint32(16).bool(message.applyToNewerVersions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceDeleteImagePropertyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceDeleteImagePropertyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imagePropertyId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.applyToNewerVersions = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceDeleteImagePropertyRequest>): ImageServiceDeleteImagePropertyRequest {
    return ImageServiceDeleteImagePropertyRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceDeleteImagePropertyRequest>): ImageServiceDeleteImagePropertyRequest {
    const message = createBaseImageServiceDeleteImagePropertyRequest();
    message.imagePropertyId = object.imagePropertyId ?? "";
    message.applyToNewerVersions = object.applyToNewerVersions ?? false;
    return message;
  },
};

function createBaseImageServiceDeleteImagePropertyResponse(): ImageServiceDeleteImagePropertyResponse {
  return { affectedImageIds: [] };
}

export const ImageServiceDeleteImagePropertyResponse: MessageFns<ImageServiceDeleteImagePropertyResponse> = {
  encode(message: ImageServiceDeleteImagePropertyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.affectedImageIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceDeleteImagePropertyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceDeleteImagePropertyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.affectedImageIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceDeleteImagePropertyResponse>): ImageServiceDeleteImagePropertyResponse {
    return ImageServiceDeleteImagePropertyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceDeleteImagePropertyResponse>): ImageServiceDeleteImagePropertyResponse {
    const message = createBaseImageServiceDeleteImagePropertyResponse();
    message.affectedImageIds = object.affectedImageIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseImageServiceCopyImagePropertiesRequest(): ImageServiceCopyImagePropertiesRequest {
  return { sourceImageId: "", targetImageIds: [], overwriteExisting: undefined };
}

export const ImageServiceCopyImagePropertiesRequest: MessageFns<ImageServiceCopyImagePropertiesRequest> = {
  encode(message: ImageServiceCopyImagePropertiesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sourceImageId !== "") {
      writer.uint32(10).string(message.sourceImageId);
    }
    for (const v of message.targetImageIds) {
      writer.uint32(18).string(v!);
    }
    if (message.overwriteExisting !== undefined) {
      writer.uint32(24).bool(message.overwriteExisting);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceCopyImagePropertiesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceCopyImagePropertiesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sourceImageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.targetImageIds.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.overwriteExisting = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceCopyImagePropertiesRequest>): ImageServiceCopyImagePropertiesRequest {
    return ImageServiceCopyImagePropertiesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceCopyImagePropertiesRequest>): ImageServiceCopyImagePropertiesRequest {
    const message = createBaseImageServiceCopyImagePropertiesRequest();
    message.sourceImageId = object.sourceImageId ?? "";
    message.targetImageIds = object.targetImageIds?.map((e) => e) || [];
    message.overwriteExisting = object.overwriteExisting ?? undefined;
    return message;
  },
};

function createBaseImageServiceCopyImagePropertiesResponse(): ImageServiceCopyImagePropertiesResponse {
  return { affectedImageIds: [], propertiesCopied: 0 };
}

export const ImageServiceCopyImagePropertiesResponse: MessageFns<ImageServiceCopyImagePropertiesResponse> = {
  encode(message: ImageServiceCopyImagePropertiesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.affectedImageIds) {
      writer.uint32(10).string(v!);
    }
    if (message.propertiesCopied !== 0) {
      writer.uint32(16).uint32(message.propertiesCopied);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageServiceCopyImagePropertiesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageServiceCopyImagePropertiesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.affectedImageIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.propertiesCopied = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ImageServiceCopyImagePropertiesResponse>): ImageServiceCopyImagePropertiesResponse {
    return ImageServiceCopyImagePropertiesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageServiceCopyImagePropertiesResponse>): ImageServiceCopyImagePropertiesResponse {
    const message = createBaseImageServiceCopyImagePropertiesResponse();
    message.affectedImageIds = object.affectedImageIds?.map((e) => e) || [];
    message.propertiesCopied = object.propertiesCopied ?? 0;
    return message;
  },
};

/** Service for managing protocol image builds. */
export type ImageServiceDefinition = typeof ImageServiceDefinition;
export const ImageServiceDefinition = {
  name: "ImageService",
  fullName: "blockjoy.v1.ImageService",
  methods: {
    /** Create a new image and set of properties for a protocol version. */
    addImage: {
      name: "AddImage",
      requestType: ImageServiceAddImageRequest,
      requestStream: false,
      responseType: ImageServiceAddImageResponse,
      responseStream: false,
      options: {},
    },
    /** Get image info for some protocol and node type. */
    getImage: {
      name: "GetImage",
      requestType: ImageServiceGetImageRequest,
      requestStream: false,
      responseType: ImageServiceGetImageResponse,
      responseStream: false,
      options: {},
    },
    /** List all image archives for some image. */
    listArchives: {
      name: "ListArchives",
      requestType: ImageServiceListArchivesRequest,
      requestStream: false,
      responseType: ImageServiceListArchivesResponse,
      responseStream: false,
      options: {},
    },
    /** Update the storage location of an image archive. */
    updateArchive: {
      name: "UpdateArchive",
      requestType: ImageServiceUpdateArchiveRequest,
      requestStream: false,
      responseType: ImageServiceUpdateArchiveResponse,
      responseStream: false,
      options: {},
    },
    /** Update the config of an existing image. */
    updateImage: {
      name: "UpdateImage",
      requestType: ImageServiceUpdateImageRequest,
      requestStream: false,
      responseType: ImageServiceUpdateImageResponse,
      responseStream: false,
      options: {},
    },
    /**
     * Admin endpoints for image management
     * List all images with basic info for admin management.
     */
    listImages: {
      name: "ListImages",
      requestType: ImageServiceListImagesRequest,
      requestStream: false,
      responseType: ImageServiceListImagesResponse,
      responseStream: false,
      options: {},
    },
    /** Get detailed image info including all properties for admin management. */
    getImageDetails: {
      name: "GetImageDetails",
      requestType: ImageServiceGetImageDetailsRequest,
      requestStream: false,
      responseType: ImageServiceGetImageDetailsResponse,
      responseStream: false,
      options: {},
    },
    /** Add a new property to an existing image. */
    addImageProperty: {
      name: "AddImageProperty",
      requestType: ImageServiceAddImagePropertyRequest,
      requestStream: false,
      responseType: ImageServiceAddImagePropertyResponse,
      responseStream: false,
      options: {},
    },
    /** Update an existing image property. */
    updateImageProperty: {
      name: "UpdateImageProperty",
      requestType: ImageServiceUpdateImagePropertyRequest,
      requestStream: false,
      responseType: ImageServiceUpdateImagePropertyResponse,
      responseStream: false,
      options: {},
    },
    /** Delete an image property. */
    deleteImageProperty: {
      name: "DeleteImageProperty",
      requestType: ImageServiceDeleteImagePropertyRequest,
      requestStream: false,
      responseType: ImageServiceDeleteImagePropertyResponse,
      responseStream: false,
      options: {},
    },
    /** Copy properties from one image version to another. */
    copyImageProperties: {
      name: "CopyImageProperties",
      requestType: ImageServiceCopyImagePropertiesRequest,
      requestStream: false,
      responseType: ImageServiceCopyImagePropertiesResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface ImageServiceImplementation<CallContextExt = {}> {
  /** Create a new image and set of properties for a protocol version. */
  addImage(
    request: ImageServiceAddImageRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceAddImageResponse>>;
  /** Get image info for some protocol and node type. */
  getImage(
    request: ImageServiceGetImageRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceGetImageResponse>>;
  /** List all image archives for some image. */
  listArchives(
    request: ImageServiceListArchivesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceListArchivesResponse>>;
  /** Update the storage location of an image archive. */
  updateArchive(
    request: ImageServiceUpdateArchiveRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceUpdateArchiveResponse>>;
  /** Update the config of an existing image. */
  updateImage(
    request: ImageServiceUpdateImageRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceUpdateImageResponse>>;
  /**
   * Admin endpoints for image management
   * List all images with basic info for admin management.
   */
  listImages(
    request: ImageServiceListImagesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceListImagesResponse>>;
  /** Get detailed image info including all properties for admin management. */
  getImageDetails(
    request: ImageServiceGetImageDetailsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceGetImageDetailsResponse>>;
  /** Add a new property to an existing image. */
  addImageProperty(
    request: ImageServiceAddImagePropertyRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceAddImagePropertyResponse>>;
  /** Update an existing image property. */
  updateImageProperty(
    request: ImageServiceUpdateImagePropertyRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceUpdateImagePropertyResponse>>;
  /** Delete an image property. */
  deleteImageProperty(
    request: ImageServiceDeleteImagePropertyRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceDeleteImagePropertyResponse>>;
  /** Copy properties from one image version to another. */
  copyImageProperties(
    request: ImageServiceCopyImagePropertiesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ImageServiceCopyImagePropertiesResponse>>;
}

export interface ImageServiceClient<CallOptionsExt = {}> {
  /** Create a new image and set of properties for a protocol version. */
  addImage(
    request: DeepPartial<ImageServiceAddImageRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceAddImageResponse>;
  /** Get image info for some protocol and node type. */
  getImage(
    request: DeepPartial<ImageServiceGetImageRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceGetImageResponse>;
  /** List all image archives for some image. */
  listArchives(
    request: DeepPartial<ImageServiceListArchivesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceListArchivesResponse>;
  /** Update the storage location of an image archive. */
  updateArchive(
    request: DeepPartial<ImageServiceUpdateArchiveRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceUpdateArchiveResponse>;
  /** Update the config of an existing image. */
  updateImage(
    request: DeepPartial<ImageServiceUpdateImageRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceUpdateImageResponse>;
  /**
   * Admin endpoints for image management
   * List all images with basic info for admin management.
   */
  listImages(
    request: DeepPartial<ImageServiceListImagesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceListImagesResponse>;
  /** Get detailed image info including all properties for admin management. */
  getImageDetails(
    request: DeepPartial<ImageServiceGetImageDetailsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceGetImageDetailsResponse>;
  /** Add a new property to an existing image. */
  addImageProperty(
    request: DeepPartial<ImageServiceAddImagePropertyRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceAddImagePropertyResponse>;
  /** Update an existing image property. */
  updateImageProperty(
    request: DeepPartial<ImageServiceUpdateImagePropertyRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceUpdateImagePropertyResponse>;
  /** Delete an image property. */
  deleteImageProperty(
    request: DeepPartial<ImageServiceDeleteImagePropertyRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceDeleteImagePropertyResponse>;
  /** Copy properties from one image version to another. */
  copyImageProperties(
    request: DeepPartial<ImageServiceCopyImagePropertiesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ImageServiceCopyImagePropertiesResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
